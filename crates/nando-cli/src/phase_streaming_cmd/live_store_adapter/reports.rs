use serde::{Deserialize, Serialize};

use super::super::ForbiddenFlags;
use super::architecture::PhaseStreamArchitectureVersionsReport;

#[derive(Clone, Debug, Default, Serialize)]
pub(super) struct LiveStoreProviderEvidenceArtifactsReport {
    pub(super) refreshed: bool,
    pub(super) refresh_error: Option<String>,
    pub(super) billing_request_rows: usize,
    pub(super) billing_request_tokens: u64,
    pub(super) billing_request_current_cost_microusd: u64,
    pub(super) provider_export_drop_path: String,
    pub(super) provider_export_present: bool,
    pub(super) provider_export_attestation_contract_report_path: String,
    pub(super) provider_export_attestation_template_path: String,
    pub(super) provider_export_attestation_contract_refreshed: bool,
    pub(super) acquisition_report_path: String,
    pub(super) acquisition_manifest_jsonl_path: String,
    pub(super) acquisition_capture_request_jsonl_path: String,
    pub(super) acquisition_required_schema_json_path: String,
    pub(super) acquisition_worklist_ready: bool,
    pub(super) acquisition_provider_boundary_correlation_ready: bool,
    pub(super) acquisition_provider_id_backfill_required: bool,
    pub(super) acquisition_market_money_claim_allowed: bool,
    pub(super) acquisition_verdict: String,
    pub(super) codex_token_backfill_report_path: String,
    pub(super) codex_token_backfill_output_provider_boundary_path: String,
    pub(super) codex_token_backfill_emitted_provider_boundary_rows: usize,
    pub(super) codex_token_backfill_appended_total_tokens: u64,
    pub(super) codex_token_backfill_full_capture_coverage: bool,
    pub(super) codex_token_backfill_verdict: String,
    pub(super) realtrace_token_cost_backfill_report_path: String,
    pub(super) realtrace_token_cost_backfill_output_provider_boundary_path: String,
    pub(super) realtrace_token_cost_backfill_emitted_provider_boundary_rows: usize,
    pub(super) realtrace_token_cost_backfill_appended_total_tokens: u64,
    pub(super) realtrace_token_cost_backfill_appended_total_cost_microusd: u64,
    pub(super) realtrace_token_cost_backfill_full_capture_coverage: bool,
    pub(super) realtrace_token_cost_backfill_verdict: String,
    pub(super) evidence_chain_report_path: String,
    pub(super) evidence_chain_provider_export_required: bool,
    pub(super) evidence_chain_provider_billing_evidence_present: bool,
    pub(super) evidence_chain_external_ready: bool,
    pub(super) evidence_chain_market_money_claim_allowed: bool,
    pub(super) evidence_chain_verdict: String,
    pub(super) capture_contract_report_path: String,
    pub(super) capture_contract_template_jsonl_path: String,
    pub(super) capture_contract_template_csv_path: String,
    pub(super) capture_contract_ready: bool,
    pub(super) capture_contract_market_money_claim_allowed: bool,
    pub(super) capture_contract_verdict: String,
    pub(super) local_accept_enabled: bool,
    pub(super) market_money_claim_allowed: bool,
    pub(super) boundary: &'static str,
}

#[derive(Clone, Debug, Serialize)]
pub(super) struct PhaseStreamHotPathBenchmarkReport {
    pub(super) report_kind: &'static str,
    pub(super) runtime_source: &'static str,
    pub(super) input_trace_paths: Vec<String>,
    pub(super) promotion_review_report_path: String,
    pub(super) daemon_admission_policy_report_path: String,
    pub(super) total_rows: usize,
    pub(super) parsed_rows: usize,
    pub(super) skipped_no_verifier_label: usize,
    pub(super) skipped_no_safe_atoms: usize,
    pub(super) route_index_missing_events: usize,
    pub(super) cells: usize,
    pub(super) min_bucket_events: usize,
    pub(super) timed_score_iterations_requested: usize,
    pub(super) timed_score_iterations: usize,
    pub(super) prepared_unique_rows: usize,
    pub(super) online_bucket_count: usize,
    pub(super) candidate_bucket_count: usize,
    pub(super) rejected_bucket_count: usize,
    pub(super) direct_hot_snapshot_capacity: usize,
    pub(super) direct_hot_snapshot_captured_count: usize,
    pub(super) direct_hot_snapshot_count: usize,
    pub(super) direct_hot_snapshot_evicted_count: usize,
    pub(super) selected_direct_hot_snapshot_index: usize,
    pub(super) selected_direct_hot_snapshot_frozen_after_parsed_rows: usize,
    pub(super) validation_score_event_target: usize,
    pub(super) selected_validation_score_events: usize,
    pub(super) selected_validation_score_candidate_events: usize,
    pub(super) selected_validation_unique_cpu_accepts_over_exact_cache: usize,
    pub(super) selected_validation_tokens_saved: u64,
    pub(super) selected_validation_cost_saved_microusd: u64,
    pub(super) selected_validation_false_accepts: usize,
    pub(super) future_eval_start_after_parsed_rows: usize,
    pub(super) future_shadow_split_used: bool,
    pub(super) future_total_tokens: u64,
    pub(super) future_total_cost_microusd: u64,
    pub(super) future_exact_cache_hits: usize,
    pub(super) future_exact_cache_tokens: u64,
    pub(super) future_exact_cache_cost_microusd: u64,
    pub(super) future_non_exact_rows: usize,
    pub(super) hot_profile_count: usize,
    pub(super) hot_route_count: usize,
    pub(super) hot_route_profile_edges: usize,
    pub(super) hot_bytes_estimate: usize,
    pub(super) hot_scratch_bytes_estimate: usize,
    pub(super) runtime_budget: PhaseStreamLiveStoreBudgetReport,
    pub(super) cold_adapter_json_used: bool,
    pub(super) cold_adapter_strings_used: bool,
    pub(super) direct_mutable_store_export_used: bool,
    pub(super) clean_manifest_loaded: bool,
    pub(super) package_roundtrip_used: bool,
    pub(super) worker_thread_used: bool,
    pub(super) bounded_sync_channel_used: bool,
    pub(super) batch_messages_used: bool,
    pub(super) hot_path_direct_runtime_used: bool,
    pub(super) hot_loop_json_used: bool,
    pub(super) hot_loop_string_route_used: bool,
    pub(super) hot_loop_btreemap_used: bool,
    pub(super) hot_loop_file_io_used: bool,
    pub(super) hot_loop_package_compile_used: bool,
    pub(super) score_events: usize,
    pub(super) score_candidate_events: usize,
    pub(super) verifier_required_events: usize,
    pub(super) local_accept_events: usize,
    pub(super) unique_cpu_accepts_over_exact_cache: usize,
    pub(super) tokens_saved: u64,
    pub(super) cost_saved_microusd: u64,
    pub(super) false_accepts: usize,
    pub(super) runtime_margin_parity_checks: usize,
    pub(super) runtime_margin_parity_mismatches: usize,
    pub(super) runtime_decision_parity_mismatches: usize,
    pub(super) exact_cache_overlap_excluded: bool,
    pub(super) token_cost_denominator_present: bool,
    pub(super) nando_calls_saved_milli: usize,
    pub(super) nando_tokens_saved_milli: usize,
    pub(super) nando_cost_saved_milli: usize,
    pub(super) p5_evidence_ready_for_promotion_audit: bool,
    pub(super) verifier_binding_bound: bool,
    pub(super) promotion_contract_evaluated: bool,
    pub(super) promotion_contract_eligible: bool,
    pub(super) promotion_contract_blocker: &'static str,
    pub(super) promotion_review_ready_not_promoted: bool,
    pub(super) product_promotion_enabled: bool,
    pub(super) hot_path_p50_latency_ns: u128,
    pub(super) hot_path_p90_latency_ns: u128,
    pub(super) hot_path_p99_latency_ns: u128,
    pub(super) hot_path_max_latency_ns: u128,
    pub(super) hot_path_margin_checksum_micro: i128,
    pub(super) local_accept_enabled: bool,
    pub(super) market_money_claim_allowed: bool,
    pub(super) verdict: &'static str,
    pub(super) blocker: &'static str,
    pub(super) forbidden_flags: ForbiddenFlags,
    pub(super) boundary: &'static str,
}

#[derive(Clone, Debug, Serialize)]
pub(super) struct PhaseStreamHotPathDaemonLiveLoopNumericBenchmarkReport {
    pub(super) report_kind: &'static str,
    pub(super) mode: &'static str,
    pub(super) watermark_trace_paths: Vec<String>,
    pub(super) append_trace_paths: Vec<String>,
    pub(super) cells: usize,
    pub(super) min_bucket_events: usize,
    pub(super) watermark_total_rows: usize,
    pub(super) watermark_parsed_rows: usize,
    pub(super) append_total_rows: usize,
    pub(super) append_parsed_rows: usize,
    pub(super) timed_events: usize,
    pub(super) active_before_update_events: usize,
    pub(super) calibration_events: usize,
    pub(super) raw_local_operator_events: usize,
    pub(super) local_operator_shadow_decisions: usize,
    pub(super) unique_cpu_accepts_over_exact_cache: usize,
    pub(super) tokens_saved: u64,
    pub(super) cost_saved_microusd: u64,
    pub(super) false_accepts: usize,
    pub(super) append_total_tokens: u64,
    pub(super) append_total_cost_microusd: u64,
    pub(super) append_exact_cache_hits: usize,
    pub(super) append_exact_cache_tokens: u64,
    pub(super) append_exact_cache_cost_microusd: u64,
    pub(super) append_non_exact_rows: usize,
    pub(super) p50_latency_ns: u128,
    pub(super) p90_latency_ns: u128,
    pub(super) p99_latency_ns: u128,
    pub(super) max_latency_ns: u128,
    pub(super) timed_lane_p99_budget_ns: u128,
    pub(super) runtime_budget: PhaseStreamLiveStoreBudgetReport,
    pub(super) hot_runtime_available_after_benchmark: bool,
    pub(super) hot_route_ids_after_benchmark: Vec<u32>,
    pub(super) hot_profile_ids_after_benchmark: Vec<u32>,
    pub(super) verifier_binding_bound: bool,
    pub(super) candidate_package_count: usize,
    pub(super) candidate_package_dir: String,
    pub(super) candidate_packages: Vec<PhaseStreamLiveStoreCandidatePackageReport>,
    pub(super) threshold_candidate_bucket_count: usize,
    pub(super) threshold_auto_calibrated_bucket_count: usize,
    pub(super) threshold_calibration_window_before_shadow: bool,
    pub(super) threshold_shadow_window_after_calibration: bool,
    pub(super) threshold_per_bucket_reported: bool,
    pub(super) threshold_fixed_policy_shadow_replay: bool,
    pub(super) promotion_evidence_eligible: bool,
    pub(super) promotion_evidence_blocker: &'static str,
    pub(super) exact_cache_overlap_excluded: bool,
    pub(super) token_cost_denominator_present: bool,
    pub(super) cold_adapter_json_used: bool,
    pub(super) cold_adapter_strings_used: bool,
    pub(super) timed_lane_json_used: bool,
    pub(super) timed_lane_string_route_used: bool,
    pub(super) timed_lane_btreemap_used: bool,
    pub(super) timed_lane_file_io_used: bool,
    pub(super) timed_lane_report_aggregation_used: bool,
    pub(super) timed_lane_package_compile_used: bool,
    pub(super) timed_lane_numeric_route_bucket_atom_ids_used: bool,
    pub(super) direct_mutable_store_used: bool,
    pub(super) score_before_update_used: bool,
    pub(super) registry_mutation_enabled: bool,
    pub(super) cpu_profile_registry_write_enabled: bool,
    pub(super) serving_profile_artifact_written: bool,
    pub(super) product_promotion_enabled: bool,
    pub(super) local_accept_enabled: bool,
    pub(super) market_money_claim_allowed: bool,
    pub(super) forbidden_flags: ForbiddenFlags,
    pub(super) verdict: &'static str,
    pub(super) blocker: String,
    pub(super) boundary: &'static str,
}

#[derive(Clone, Debug, Serialize)]
pub(super) struct PhaseStreamHotPathDaemonNumericPackageShadowAuditReport {
    pub(super) report_kind: &'static str,
    pub(super) mode: &'static str,
    pub(super) source_numeric_report_path: String,
    pub(super) candidate_package_path: String,
    pub(super) watermark_trace_paths: Vec<String>,
    pub(super) append_trace_paths: Vec<String>,
    pub(super) cells: usize,
    pub(super) min_bucket_events: usize,
    pub(super) candidate_bucket_id: u32,
    pub(super) candidate_route_id: u32,
    pub(super) package_fingerprint64: u64,
    pub(super) package_bytes: usize,
    pub(super) package_record_count: usize,
    pub(super) threshold_micro: i64,
    pub(super) package_matches_report: bool,
    pub(super) verifier_binding_bound: bool,
    pub(super) watermark_parsed_rows: usize,
    pub(super) append_parsed_rows: usize,
    pub(super) append_matching_bucket_events: usize,
    pub(super) append_non_matching_bucket_events: usize,
    pub(super) route_index_missing_events: usize,
    pub(super) score_events: usize,
    pub(super) score_candidate_events: usize,
    pub(super) verifier_required_events: usize,
    pub(super) local_accept_events: usize,
    pub(super) unique_cpu_accepts_over_exact_cache: usize,
    pub(super) tokens_saved: u64,
    pub(super) cost_saved_microusd: u64,
    pub(super) false_accepts: usize,
    pub(super) runtime_margin_parity_checks: usize,
    pub(super) runtime_margin_parity_mismatches: usize,
    pub(super) runtime_decision_parity_mismatches: usize,
    pub(super) append_total_tokens: u64,
    pub(super) append_total_cost_microusd: u64,
    pub(super) append_exact_cache_hits: usize,
    pub(super) append_exact_cache_tokens: u64,
    pub(super) append_exact_cache_cost_microusd: u64,
    pub(super) append_non_exact_rows: usize,
    pub(super) p50_latency_ns: u128,
    pub(super) p90_latency_ns: u128,
    pub(super) p99_latency_ns: u128,
    pub(super) max_latency_ns: u128,
    pub(super) p99_budget_ns: u128,
    pub(super) atom_worker_p50_latency_ns: u128,
    pub(super) atom_worker_p90_latency_ns: u128,
    pub(super) atom_worker_p99_latency_ns: u128,
    pub(super) atom_worker_max_latency_ns: u128,
    pub(super) hot_profile_count: usize,
    pub(super) hot_route_count: usize,
    pub(super) hot_route_profile_edges: usize,
    pub(super) hot_bytes_estimate: usize,
    pub(super) exact_cache_overlap_excluded: bool,
    pub(super) token_cost_denominator_present: bool,
    pub(super) package_loaded_into_hot_runtime: bool,
    pub(super) source_numeric_gate_passed: bool,
    pub(super) source_promotion_evidence_eligible: bool,
    pub(super) source_local_accept_enabled: bool,
    pub(super) source_market_money_claim_allowed: bool,
    pub(super) market_savings_count_allowed: bool,
    pub(super) product_promotion_enabled: bool,
    pub(super) local_accept_enabled: bool,
    pub(super) market_money_claim_allowed: bool,
    pub(super) forbidden_flags: ForbiddenFlags,
    pub(super) verdict: &'static str,
    pub(super) blocker: String,
    pub(super) boundary: &'static str,
}

#[derive(Clone, Debug, Serialize)]
pub(super) struct PhaseStreamHotPathDaemonNumericFuturePackageAuditReport {
    pub(super) report_kind: &'static str,
    pub(super) mode: &'static str,
    pub(super) daemon_admission_policy_report_path: String,
    pub(super) watermark_trace_paths: Vec<String>,
    pub(super) append_trace_paths: Vec<String>,
    pub(super) cells: usize,
    pub(super) min_bucket_events: usize,
    pub(super) watermark_parsed_rows: usize,
    pub(super) append_parsed_rows: usize,
    pub(super) freeze_after_append_events: usize,
    pub(super) candidate_bucket_id: u32,
    pub(super) candidate_route_id: u32,
    pub(super) candidate_package_path: String,
    pub(super) package_fingerprint64: u64,
    pub(super) package_bytes: usize,
    pub(super) package_record_count: usize,
    pub(super) threshold_micro: i64,
    pub(super) verifier_binding_bound: bool,
    pub(super) future_total_events_after_freeze: usize,
    pub(super) future_matching_bucket_events: usize,
    pub(super) future_non_matching_bucket_events: usize,
    pub(super) score_events: usize,
    pub(super) score_candidate_events: usize,
    pub(super) verifier_required_events: usize,
    pub(super) local_accept_events: usize,
    pub(super) unique_cpu_accepts_over_exact_cache: usize,
    pub(super) tokens_saved: u64,
    pub(super) cost_saved_microusd: u64,
    pub(super) false_accepts: usize,
    pub(super) runtime_margin_parity_checks: usize,
    pub(super) runtime_margin_parity_mismatches: usize,
    pub(super) runtime_decision_parity_mismatches: usize,
    pub(super) future_total_tokens: u64,
    pub(super) future_total_cost_microusd: u64,
    pub(super) future_exact_cache_hits: usize,
    pub(super) future_exact_cache_tokens: u64,
    pub(super) future_exact_cache_cost_microusd: u64,
    pub(super) future_non_exact_rows: usize,
    pub(super) p50_latency_ns: u128,
    pub(super) p90_latency_ns: u128,
    pub(super) p99_latency_ns: u128,
    pub(super) max_latency_ns: u128,
    pub(super) p99_budget_ns: u128,
    pub(super) hot_profile_count: usize,
    pub(super) hot_route_count: usize,
    pub(super) hot_route_profile_edges: usize,
    pub(super) hot_bytes_estimate: usize,
    pub(super) exact_cache_overlap_excluded: bool,
    pub(super) token_cost_denominator_present: bool,
    pub(super) fresh_future_split_used: bool,
    pub(super) fresh_future_savings_evidence_allowed: bool,
    pub(super) product_promotion_enabled: bool,
    pub(super) local_accept_enabled: bool,
    pub(super) market_money_claim_allowed: bool,
    pub(super) forbidden_flags: ForbiddenFlags,
    pub(super) verdict: &'static str,
    pub(super) blocker: String,
    pub(super) boundary: &'static str,
}

#[derive(Clone, Debug, Serialize)]
pub(super) struct PhaseStreamHotPathDaemonNumericFalseAcceptSplitAuditReport {
    pub(super) report_kind: &'static str,
    pub(super) mode: &'static str,
    pub(super) source_future_audit_report_path: String,
    pub(super) candidate_package_path: String,
    pub(super) append_trace_paths: Vec<String>,
    pub(super) cells: usize,
    pub(super) min_bucket_events: usize,
    pub(super) top_k: usize,
    pub(super) append_total_rows: usize,
    pub(super) append_parsed_rows: usize,
    pub(super) append_skipped_no_verifier_label: usize,
    pub(super) append_skipped_no_safe_atoms: usize,
    pub(super) candidate_route_id: u32,
    pub(super) candidate_bucket_id: u32,
    pub(super) threshold_micro: i64,
    pub(super) freeze_after_append_events: usize,
    pub(super) future_events_after_freeze: usize,
    pub(super) future_matching_bucket_events: usize,
    pub(super) score_events: usize,
    pub(super) score_candidate_events: usize,
    pub(super) unique_cpu_accepts_over_exact_cache: usize,
    pub(super) tokens_saved: u64,
    pub(super) cost_saved_microusd: u64,
    pub(super) false_accepts: usize,
    pub(super) clean_atom_candidate_count: usize,
    pub(super) stageable_clean_atom_candidate_count: usize,
    pub(super) best_clean_atom_unique_cpu_accepts_over_exact_cache: usize,
    pub(super) best_clean_atom_tokens_saved: u64,
    pub(super) best_clean_atom_cost_saved_microusd: u64,
    pub(super) best_stageable_clean_atom_unique_cpu_accepts_over_exact_cache: usize,
    pub(super) best_stageable_clean_atom_tokens_saved: u64,
    pub(super) best_stageable_clean_atom_cost_saved_microusd: u64,
    pub(super) top_clean_atoms: Vec<PhaseStreamHotPathDaemonNumericFalseAcceptAtomReport>,
    pub(super) top_stageable_clean_atoms: Vec<PhaseStreamHotPathDaemonNumericFalseAcceptAtomReport>,
    pub(super) top_false_atoms: Vec<PhaseStreamHotPathDaemonNumericFalseAcceptAtomReport>,
    pub(super) runtime_margin_parity_checks: usize,
    pub(super) runtime_margin_parity_mismatches: usize,
    pub(super) runtime_decision_parity_mismatches: usize,
    pub(super) p50_latency_ns: u128,
    pub(super) p90_latency_ns: u128,
    pub(super) p99_latency_ns: u128,
    pub(super) max_latency_ns: u128,
    pub(super) p99_budget_ns: u128,
    pub(super) split_diagnostic_ready: bool,
    pub(super) product_promotion_enabled: bool,
    pub(super) local_accept_enabled: bool,
    pub(super) market_money_claim_allowed: bool,
    pub(super) forbidden_flags: ForbiddenFlags,
    pub(super) verdict: &'static str,
    pub(super) blocker: String,
    pub(super) boundary: &'static str,
}

#[derive(Clone, Debug, Serialize)]
pub(super) struct PhaseStreamHotPathDaemonNumericFalseAcceptAtomReport {
    pub(super) atom_id: u64,
    pub(super) atom: String,
    pub(super) score_candidate_events: usize,
    pub(super) unique_cpu_accepts_over_exact_cache: usize,
    pub(super) tokens_saved: u64,
    pub(super) cost_saved_microusd: u64,
    pub(super) false_accepts: usize,
    pub(super) false_accept_milli: u64,
    pub(super) clean_split_candidate: bool,
    pub(super) stageable_clean_split_candidate: bool,
    pub(super) refinement_blocker: &'static str,
}

#[derive(Clone, Debug, Serialize)]
pub(super) struct PhaseStreamHotPathPromotionReviewReport {
    pub(super) report_kind: &'static str,
    pub(super) mode: &'static str,
    pub(super) source_benchmark_report_path: String,
    pub(super) daemon_admission_policy_report_path: String,
    pub(super) runtime_source: &'static str,
    pub(super) input_trace_paths: Vec<String>,
    pub(super) cells: usize,
    pub(super) min_bucket_events: usize,
    pub(super) selected_direct_hot_snapshot_index: usize,
    pub(super) selected_direct_hot_snapshot_frozen_after_parsed_rows: usize,
    pub(super) future_eval_start_after_parsed_rows: usize,
    pub(super) future_shadow_split_used: bool,
    pub(super) hot_route_ids: Vec<u32>,
    pub(super) hot_profile_ids: Vec<u32>,
    pub(super) hot_profile_count: usize,
    pub(super) hot_route_count: usize,
    pub(super) hot_route_profile_edges: usize,
    pub(super) hot_bytes_estimate: usize,
    pub(super) score_events: usize,
    pub(super) score_candidate_events: usize,
    pub(super) unique_cpu_accepts_over_exact_cache: usize,
    pub(super) tokens_saved: u64,
    pub(super) cost_saved_microusd: u64,
    pub(super) false_accepts: usize,
    pub(super) runtime_margin_parity_checks: usize,
    pub(super) runtime_margin_parity_mismatches: usize,
    pub(super) runtime_decision_parity_mismatches: usize,
    pub(super) exact_cache_overlap_excluded: bool,
    pub(super) token_cost_denominator_present: bool,
    pub(super) verifier_binding_bound: bool,
    pub(super) verifier_id: u32,
    pub(super) verifier_version: u32,
    pub(super) verifier_input_kind_id: u32,
    pub(super) verifier_evidence_source_id: u32,
    pub(super) verifier_false_accept_threshold: usize,
    pub(super) p5_evidence_ready_for_promotion_audit: bool,
    pub(super) promotion_contract_eligible: bool,
    pub(super) promotion_contract_blocker: &'static str,
    pub(super) promotion_review_candidate_allowed: bool,
    pub(super) shadow_only_daemon_admission_review_allowed: bool,
    pub(super) product_promotion_enabled: bool,
    pub(super) local_accept_enabled: bool,
    pub(super) market_money_claim_allowed: bool,
    pub(super) verdict: &'static str,
    pub(super) blocker: &'static str,
    pub(super) forbidden_flags: ForbiddenFlags,
    pub(super) boundary: &'static str,
}

#[derive(Clone, Debug, Serialize)]
pub(super) struct PhaseStreamHotPathDaemonAdmissionPolicyReport {
    pub(super) report_kind: &'static str,
    pub(super) mode: &'static str,
    pub(super) source_benchmark_report_path: String,
    pub(super) source_promotion_review_report_path: String,
    pub(super) admission_policy_kind: &'static str,
    pub(super) runtime_source: &'static str,
    pub(super) hot_route_ids: Vec<u32>,
    pub(super) hot_profile_ids: Vec<u32>,
    pub(super) hot_profile_count: usize,
    pub(super) hot_route_count: usize,
    pub(super) hot_route_profile_edges: usize,
    pub(super) hot_bytes_estimate: usize,
    pub(super) future_shadow_split_used: bool,
    pub(super) verifier_binding_bound: bool,
    pub(super) exact_cache_overlap_excluded: bool,
    pub(super) token_cost_denominator_present: bool,
    pub(super) unique_cpu_accepts_over_exact_cache: usize,
    pub(super) tokens_saved: u64,
    pub(super) cost_saved_microusd: u64,
    pub(super) false_accepts: usize,
    pub(super) runtime_margin_parity_mismatches: usize,
    pub(super) runtime_decision_parity_mismatches: usize,
    pub(super) promotion_review_candidate_allowed: bool,
    pub(super) shadow_only_daemon_admission_review_allowed: bool,
    pub(super) admission_policy_candidate_allowed: bool,
    pub(super) registry_mutation_enabled: bool,
    pub(super) cpu_profile_registry_write_enabled: bool,
    pub(super) serving_profile_artifact_written: bool,
    pub(super) product_promotion_enabled: bool,
    pub(super) local_accept_enabled: bool,
    pub(super) market_money_claim_allowed: bool,
    pub(super) verdict: &'static str,
    pub(super) blocker: &'static str,
    pub(super) forbidden_flags: ForbiddenFlags,
    pub(super) boundary: &'static str,
}

#[derive(Clone, Debug, Serialize)]
pub(super) struct PhaseStreamHotPathDaemonAdmissionPolicySmokeReport {
    pub(super) report_kind: &'static str,
    pub(super) mode: &'static str,
    pub(super) source_daemon_admission_policy_report_path: String,
    pub(super) source_benchmark_report_path: String,
    pub(super) source_promotion_review_report_path: String,
    pub(super) admission_policy_kind: String,
    pub(super) hot_route_ids: Vec<u32>,
    pub(super) hot_profile_ids: Vec<u32>,
    pub(super) hot_profile_count: usize,
    pub(super) hot_route_count: usize,
    pub(super) hot_route_profile_edges: usize,
    pub(super) hot_bytes_estimate: usize,
    pub(super) future_shadow_split_used: bool,
    pub(super) verifier_binding_bound: bool,
    pub(super) exact_cache_overlap_excluded: bool,
    pub(super) token_cost_denominator_present: bool,
    pub(super) unique_cpu_accepts_over_exact_cache: usize,
    pub(super) tokens_saved: u64,
    pub(super) cost_saved_microusd: u64,
    pub(super) false_accepts: usize,
    pub(super) runtime_margin_parity_mismatches: usize,
    pub(super) runtime_decision_parity_mismatches: usize,
    pub(super) promotion_review_candidate_allowed: bool,
    pub(super) shadow_only_daemon_admission_review_allowed: bool,
    pub(super) admission_policy_candidate_allowed: bool,
    pub(super) policy_decision: &'static str,
    pub(super) would_stage_for_daemon_shadow_only: bool,
    pub(super) registry_mutation_enabled: bool,
    pub(super) cpu_profile_registry_write_enabled: bool,
    pub(super) serving_profile_artifact_written: bool,
    pub(super) product_promotion_enabled: bool,
    pub(super) local_accept_enabled: bool,
    pub(super) market_money_claim_allowed: bool,
    pub(super) forbidden_flags: ForbiddenFlags,
    pub(super) guard: PhaseStreamHotPathDaemonAdmissionPolicySmokeGuard,
    pub(super) verdict: &'static str,
    pub(super) blocker: String,
    pub(super) boundary: &'static str,
}

#[derive(Clone, Debug, Serialize)]
pub(super) struct PhaseStreamHotPathDaemonAdmissionPolicySmokeGuard {
    pub(super) report_kind_matches: bool,
    pub(super) verdict_ready: bool,
    pub(super) blocker_none: bool,
    pub(super) mode_shadow_only_no_runtime_mutation: bool,
    pub(super) admission_policy_kind_matches: bool,
    pub(super) has_hot_route_ids: bool,
    pub(super) has_hot_profile_ids: bool,
    pub(super) future_shadow_split_used: bool,
    pub(super) verifier_binding_bound: bool,
    pub(super) exact_cache_overlap_excluded: bool,
    pub(super) token_cost_denominator_present: bool,
    pub(super) unique_cpu_accepts_positive: bool,
    pub(super) tokens_saved_positive: bool,
    pub(super) cost_saved_positive: bool,
    pub(super) false_accepts_zero: bool,
    pub(super) runtime_margin_parity_zero: bool,
    pub(super) runtime_decision_parity_zero: bool,
    pub(super) promotion_review_candidate_allowed: bool,
    pub(super) shadow_only_daemon_admission_review_allowed: bool,
    pub(super) admission_policy_candidate_allowed: bool,
    pub(super) registry_mutation_disabled: bool,
    pub(super) cpu_profile_registry_write_disabled: bool,
    pub(super) serving_profile_artifact_not_written: bool,
    pub(super) product_promotion_disabled: bool,
    pub(super) local_accept_disabled: bool,
    pub(super) market_money_claim_disabled: bool,
    pub(super) forbidden_flags_clear: bool,
}

#[derive(Clone, Debug, Serialize)]
pub(super) struct PhaseStreamHotPathDaemonNumericAdmissionPortfolioGateReport {
    pub(super) report_kind: &'static str,
    pub(super) mode: &'static str,
    pub(super) input_future_audit_report_paths: Vec<String>,
    pub(super) accepted_report_count: usize,
    pub(super) rejected_report_count: usize,
    pub(super) accepted_unique_cpu_accepts_over_exact_cache: usize,
    pub(super) accepted_tokens_saved: u64,
    pub(super) accepted_cost_saved_microusd: u64,
    pub(super) accepted_false_accepts: usize,
    pub(super) accepted_hot_bytes_estimate: usize,
    pub(super) max_accepted_p99_latency_ns: u128,
    pub(super) shadow_evidence_ready: bool,
    pub(super) market_money_claim_allowed: bool,
    pub(super) product_promotion_enabled: bool,
    pub(super) local_accept_enabled: bool,
    pub(super) registry_mutation_enabled: bool,
    pub(super) cpu_profile_registry_write_enabled: bool,
    pub(super) serving_profile_artifact_written: bool,
    pub(super) admitted_reports:
        Vec<PhaseStreamHotPathDaemonNumericAdmissionPortfolioAcceptedReport>,
    pub(super) rejected_reports:
        Vec<PhaseStreamHotPathDaemonNumericAdmissionPortfolioRejectedReport>,
    pub(super) forbidden_flags: ForbiddenFlags,
    pub(super) verdict: &'static str,
    pub(super) blocker: &'static str,
    pub(super) boundary: &'static str,
}

#[derive(Clone, Debug, Serialize)]
pub(super) struct PhaseStreamHotPathDaemonNumericAdmissionPortfolioAcceptedReport {
    pub(super) future_audit_report_path: String,
    pub(super) policy_smoke_report_path: String,
    pub(super) candidate_package_path: String,
    pub(super) candidate_route_id: u32,
    pub(super) candidate_bucket_id: u32,
    pub(super) unique_cpu_accepts_over_exact_cache: usize,
    pub(super) tokens_saved: u64,
    pub(super) cost_saved_microusd: u64,
    pub(super) false_accepts: usize,
    pub(super) p99_latency_ns: u128,
    pub(super) hot_bytes_estimate: usize,
}

#[derive(Clone, Debug, Serialize)]
pub(super) struct PhaseStreamHotPathDaemonNumericAdmissionPortfolioRejectedReport {
    pub(super) future_audit_report_path: String,
    pub(super) policy_smoke_report_path: String,
    pub(super) reason: String,
    pub(super) future_verdict: String,
    pub(super) future_blocker: String,
    pub(super) policy_verdict: String,
    pub(super) policy_blocker: String,
    pub(super) unique_cpu_accepts_over_exact_cache: usize,
    pub(super) tokens_saved: u64,
    pub(super) cost_saved_microusd: u64,
    pub(super) false_accepts: usize,
    pub(super) token_cost_denominator_present: bool,
}

#[derive(Clone, Debug, Serialize)]
pub(super) struct PhaseStreamHotPathDaemonNumericAdmissionPortfolioRuntimeReplayReport {
    pub(super) report_kind: &'static str,
    pub(super) mode: &'static str,
    pub(super) source_portfolio_gate_report_path: String,
    pub(super) portfolio_gate_passed: bool,
    pub(super) portfolio_shadow_evidence_ready: bool,
    pub(super) portfolio_admitted_report_count: usize,
    pub(super) replayed_report_count: usize,
    pub(super) replay_failed_report_count: usize,
    pub(super) total_score_events: usize,
    pub(super) total_score_candidate_events: usize,
    pub(super) total_unique_cpu_accepts_over_exact_cache: usize,
    pub(super) total_tokens_saved: u64,
    pub(super) total_cost_saved_microusd: u64,
    pub(super) total_false_accepts: usize,
    pub(super) total_runtime_margin_parity_checks: usize,
    pub(super) total_runtime_margin_parity_mismatches: usize,
    pub(super) total_runtime_decision_parity_mismatches: usize,
    pub(super) expected_unique_cpu_accepts_over_exact_cache: usize,
    pub(super) expected_tokens_saved: u64,
    pub(super) expected_cost_saved_microusd: u64,
    pub(super) expected_false_accepts: usize,
    pub(super) total_mismatched_reports: usize,
    pub(super) replay_reports:
        Vec<PhaseStreamHotPathDaemonNumericAdmissionPortfolioRuntimeReplayItemReport>,
    pub(super) local_accept_enabled: bool,
    pub(super) market_money_claim_allowed: bool,
    pub(super) product_promotion_enabled: bool,
    pub(super) registry_mutation_enabled: bool,
    pub(super) cpu_profile_registry_write_enabled: bool,
    pub(super) serving_profile_artifact_written: bool,
    pub(super) forbidden_flags: ForbiddenFlags,
    pub(super) verdict: &'static str,
    pub(super) blocker: &'static str,
    pub(super) boundary: &'static str,
}

#[derive(Clone, Debug, Serialize)]
pub(super) struct PhaseStreamHotPathDaemonNumericAdmissionPortfolioRuntimeReplayItemReport {
    pub(super) future_audit_report_path: String,
    pub(super) candidate_package_path: String,
    pub(super) candidate_route_id: u32,
    pub(super) candidate_bucket_id: u32,
    pub(super) threshold_micro: i64,
    pub(super) freeze_after_append_events: usize,
    pub(super) future_matching_bucket_events: usize,
    pub(super) score_events: usize,
    pub(super) score_candidate_events: usize,
    pub(super) unique_cpu_accepts_over_exact_cache: usize,
    pub(super) tokens_saved: u64,
    pub(super) cost_saved_microusd: u64,
    pub(super) false_accepts: usize,
    pub(super) runtime_margin_parity_checks: usize,
    pub(super) runtime_margin_parity_mismatches: usize,
    pub(super) runtime_decision_parity_mismatches: usize,
    pub(super) expected_score_events: usize,
    pub(super) expected_score_candidate_events: usize,
    pub(super) expected_unique_cpu_accepts_over_exact_cache: usize,
    pub(super) expected_tokens_saved: u64,
    pub(super) expected_cost_saved_microusd: u64,
    pub(super) expected_false_accepts: usize,
    pub(super) matches_future_audit: bool,
    pub(super) replay_loaded: bool,
    pub(super) blocker: String,
}

#[derive(Clone, Debug, Serialize)]
pub(super) struct PhaseStreamHotPathDaemonNumericFuturePortfolioAuditReport {
    pub(super) report_kind: &'static str,
    pub(super) mode: &'static str,
    pub(super) generated_future_audit_report_paths: Vec<String>,
    pub(super) generated_policy_smoke_report_paths: Vec<String>,
    pub(super) portfolio_gate_report_path: String,
    pub(super) runtime_replay_report_path: String,
    pub(super) cells: usize,
    pub(super) min_bucket_events: usize,
    pub(super) watermark_trace_path: String,
    pub(super) append_trace_paths: Vec<String>,
    pub(super) generated_future_audit_count: usize,
    pub(super) generated_policy_smoke_count: usize,
    pub(super) portfolio_accepted_report_count: usize,
    pub(super) portfolio_rejected_report_count: usize,
    pub(super) portfolio_shadow_evidence_ready: bool,
    pub(super) runtime_replayed_report_count: usize,
    pub(super) runtime_replay_failed_report_count: usize,
    pub(super) accepted_unique_cpu_accepts_over_exact_cache: usize,
    pub(super) accepted_tokens_saved: u64,
    pub(super) accepted_cost_saved_microusd: u64,
    pub(super) accepted_false_accepts: usize,
    pub(super) runtime_total_unique_cpu_accepts_over_exact_cache: usize,
    pub(super) runtime_total_tokens_saved: u64,
    pub(super) runtime_total_cost_saved_microusd: u64,
    pub(super) runtime_total_false_accepts: usize,
    pub(super) runtime_total_margin_parity_mismatches: usize,
    pub(super) runtime_total_decision_parity_mismatches: usize,
    pub(super) runtime_total_mismatched_reports: usize,
    pub(super) product_promotion_enabled: bool,
    pub(super) local_accept_enabled: bool,
    pub(super) market_money_claim_allowed: bool,
    pub(super) registry_mutation_enabled: bool,
    pub(super) cpu_profile_registry_write_enabled: bool,
    pub(super) serving_profile_artifact_written: bool,
    pub(super) forbidden_flags: ForbiddenFlags,
    pub(super) verdict: &'static str,
    pub(super) blocker: &'static str,
    pub(super) boundary: &'static str,
}

#[derive(Clone, Debug, Serialize)]
pub(super) struct PhaseStreamHotPathDaemonShadowGateReport {
    pub(super) report_kind: &'static str,
    pub(super) mode: &'static str,
    pub(super) source_policy_smoke_report_path: String,
    pub(super) decision_log_path: String,
    pub(super) input_trace_paths: Vec<String>,
    pub(super) cells: usize,
    pub(super) min_bucket_events: usize,
    pub(super) total_rows: usize,
    pub(super) parsed_rows: usize,
    pub(super) skipped_no_verifier_label: usize,
    pub(super) skipped_no_safe_atoms: usize,
    pub(super) online_bucket_count: usize,
    pub(super) candidate_bucket_count: usize,
    pub(super) rejected_bucket_count: usize,
    pub(super) direct_hot_snapshot_capacity: usize,
    pub(super) direct_hot_snapshot_captured_count: usize,
    pub(super) direct_hot_snapshot_count: usize,
    pub(super) direct_hot_snapshot_evicted_count: usize,
    pub(super) selected_direct_hot_snapshot_index: usize,
    pub(super) selected_direct_hot_snapshot_frozen_after_parsed_rows: usize,
    pub(super) future_eval_start_after_parsed_rows: usize,
    pub(super) future_shadow_split_used: bool,
    pub(super) staged_hot_route_ids: Vec<u32>,
    pub(super) staged_hot_profile_ids: Vec<u32>,
    pub(super) observed_hot_route_ids: Vec<u32>,
    pub(super) observed_hot_profile_ids: Vec<u32>,
    pub(super) staged_hot_route_ids_match: bool,
    pub(super) staged_hot_profile_ids_match: bool,
    pub(super) route_index_missing_events: usize,
    pub(super) score_events: usize,
    pub(super) score_candidate_events: usize,
    pub(super) verifier_required_events: usize,
    pub(super) local_accept_events: usize,
    pub(super) unique_cpu_accepts_over_exact_cache: usize,
    pub(super) tokens_saved: u64,
    pub(super) cost_saved_microusd: u64,
    pub(super) false_accepts: usize,
    pub(super) future_total_tokens: u64,
    pub(super) future_total_cost_microusd: u64,
    pub(super) future_exact_cache_hits: usize,
    pub(super) future_exact_cache_tokens: u64,
    pub(super) future_exact_cache_cost_microusd: u64,
    pub(super) future_non_exact_rows: usize,
    pub(super) exact_cache_overlap_excluded: bool,
    pub(super) token_cost_denominator_present: bool,
    pub(super) runtime_margin_parity_checks: usize,
    pub(super) runtime_margin_parity_mismatches: usize,
    pub(super) runtime_decision_parity_mismatches: usize,
    pub(super) policy_smoke_guard_passed: bool,
    pub(super) would_stage_for_daemon_shadow_only: bool,
    pub(super) registry_mutation_enabled: bool,
    pub(super) cpu_profile_registry_write_enabled: bool,
    pub(super) serving_profile_artifact_written: bool,
    pub(super) product_promotion_enabled: bool,
    pub(super) local_accept_enabled: bool,
    pub(super) market_money_claim_allowed: bool,
    pub(super) forbidden_flags: ForbiddenFlags,
    pub(super) verdict: &'static str,
    pub(super) blocker: String,
    pub(super) boundary: &'static str,
}

#[derive(Clone, Debug, Serialize)]
pub(super) struct PhaseStreamHotPathDaemonAppendShadowGateReport {
    pub(super) report_kind: &'static str,
    pub(super) mode: &'static str,
    pub(super) source_policy_smoke_report_path: String,
    pub(super) decision_log_path: String,
    pub(super) watermark_trace_paths: Vec<String>,
    pub(super) append_trace_paths: Vec<String>,
    pub(super) cells: usize,
    pub(super) min_bucket_events: usize,
    pub(super) watermark_total_rows: usize,
    pub(super) watermark_parsed_rows: usize,
    pub(super) watermark_skipped_no_verifier_label: usize,
    pub(super) watermark_skipped_no_safe_atoms: usize,
    pub(super) append_total_rows: usize,
    pub(super) append_parsed_rows: usize,
    pub(super) append_skipped_no_verifier_label: usize,
    pub(super) append_skipped_no_safe_atoms: usize,
    pub(super) online_bucket_count: usize,
    pub(super) candidate_bucket_count: usize,
    pub(super) rejected_bucket_count: usize,
    pub(super) direct_hot_snapshot_capacity: usize,
    pub(super) direct_hot_snapshot_captured_count: usize,
    pub(super) direct_hot_snapshot_count: usize,
    pub(super) direct_hot_snapshot_evicted_count: usize,
    pub(super) selected_direct_hot_snapshot_index: usize,
    pub(super) selected_direct_hot_snapshot_frozen_after_parsed_rows: usize,
    pub(super) future_eval_start_after_parsed_rows: usize,
    pub(super) watermark_future_shadow_split_used: bool,
    pub(super) append_shadow_window_used: bool,
    pub(super) staged_hot_route_ids: Vec<u32>,
    pub(super) staged_hot_profile_ids: Vec<u32>,
    pub(super) observed_hot_route_ids: Vec<u32>,
    pub(super) observed_hot_profile_ids: Vec<u32>,
    pub(super) staged_hot_route_ids_match: bool,
    pub(super) staged_hot_profile_ids_match: bool,
    pub(super) append_route_index_missing_events: usize,
    pub(super) append_score_events: usize,
    pub(super) append_score_candidate_events: usize,
    pub(super) verifier_required_events: usize,
    pub(super) local_accept_events: usize,
    pub(super) unique_cpu_accepts_over_exact_cache: usize,
    pub(super) tokens_saved: u64,
    pub(super) cost_saved_microusd: u64,
    pub(super) false_accepts: usize,
    pub(super) append_total_tokens: u64,
    pub(super) append_total_cost_microusd: u64,
    pub(super) append_exact_cache_hits: usize,
    pub(super) append_exact_cache_tokens: u64,
    pub(super) append_exact_cache_cost_microusd: u64,
    pub(super) append_non_exact_rows: usize,
    pub(super) exact_cache_overlap_excluded: bool,
    pub(super) token_cost_denominator_present: bool,
    pub(super) runtime_margin_parity_checks: usize,
    pub(super) runtime_margin_parity_mismatches: usize,
    pub(super) runtime_decision_parity_mismatches: usize,
    pub(super) policy_smoke_guard_passed: bool,
    pub(super) would_stage_for_daemon_shadow_only: bool,
    pub(super) registry_mutation_enabled: bool,
    pub(super) cpu_profile_registry_write_enabled: bool,
    pub(super) serving_profile_artifact_written: bool,
    pub(super) product_promotion_enabled: bool,
    pub(super) local_accept_enabled: bool,
    pub(super) market_money_claim_allowed: bool,
    pub(super) forbidden_flags: ForbiddenFlags,
    pub(super) verdict: &'static str,
    pub(super) blocker: String,
    pub(super) boundary: &'static str,
}

#[derive(Clone, Debug, Serialize)]
pub(super) struct PhaseStreamHotPathDaemonAppendLiveTailReport {
    pub(super) report_kind: &'static str,
    pub(super) architecture_versions: PhaseStreamArchitectureVersionsReport,
    pub(super) mode: &'static str,
    pub(super) snapshot_in_progress: bool,
    pub(super) decision_log_path: String,
    pub(super) watermark_trace_path: String,
    pub(super) append_tail_path: String,
    pub(super) cells: usize,
    pub(super) min_bucket_events: usize,
    pub(super) idle_sleep_ms: u64,
    pub(super) max_idle_ms: u64,
    pub(super) max_append_events: usize,
    pub(super) idle_elapsed_ms: u64,
    pub(super) miner_saturation_control_enabled: bool,
    pub(super) miner_saturation_min_idle_heartbeats: usize,
    pub(super) miner_saturation_sleep_ms: u64,
    pub(super) miner_saturation_idle_heartbeats: usize,
    pub(super) miner_saturation_active: bool,
    pub(super) miner_saturation_sleep_events: usize,
    pub(super) miner_saturation_last_sleep_ms: u64,
    pub(super) miner_saturation_last_append_parsed_rows: usize,
    pub(super) miner_saturation_last_score_events: usize,
    pub(super) miner_saturation_last_unique_cpu_accepts_over_exact_cache: usize,
    pub(super) miner_saturation_last_tokens_saved: u64,
    pub(super) miner_saturation_last_false_accepts: usize,
    pub(super) miner_saturation_last_bucket_count: usize,
    pub(super) miner_saturation_last_active_bucket_count: usize,
    pub(super) miner_saturation_last_refinement_count: usize,
    pub(super) miner_saturation_last_quarantined_profile_count: usize,
    pub(super) miner_active_batch_rows: usize,
    pub(super) miner_active_batch_sleep_ms: u64,
    pub(super) miner_active_batch_sleep_events: usize,
    pub(super) cold_artifact_refresh_interval_secs: u64,
    pub(super) cold_artifact_refresh_count: usize,
    pub(super) cold_artifact_refresh_used_this_snapshot: bool,
    pub(super) cold_artifact_refresh_append_rows: usize,
    pub(super) decision_log_flush_interval_rows: usize,
    pub(super) decision_log_flush_count: usize,
    pub(super) decision_log_pending_rows: usize,
    pub(super) start_at_end: bool,
    pub(super) watermark_total_rows: usize,
    pub(super) watermark_parsed_rows: usize,
    pub(super) watermark_skipped_no_verifier_label: usize,
    pub(super) watermark_skipped_no_safe_atoms: usize,
    pub(super) append_history_warm_total_rows: usize,
    pub(super) append_history_warm_parsed_rows: usize,
    pub(super) append_history_warm_skipped_no_verifier_label: usize,
    pub(super) append_history_warm_skipped_no_safe_atoms: usize,
    pub(super) tail_total_lines_seen: usize,
    pub(super) append_parsed_rows: usize,
    pub(super) append_skipped_no_verifier_label: usize,
    pub(super) append_skipped_no_safe_atoms: usize,
    pub(super) append_hot_view_available_events: usize,
    pub(super) append_route_index_missing_events: usize,
    pub(super) append_route_index_missing_before_first_score: usize,
    pub(super) append_route_index_missing_after_first_score: usize,
    pub(super) append_auto_subcenter_observe_events: usize,
    pub(super) append_auto_subcenter_throttled_events: usize,
    pub(super) append_hidden_state_subcenter_observe_events: usize,
    pub(super) miner_discovery_sample_permille: usize,
    pub(super) miner_clean_hot_runtime_throttle_events: usize,
    pub(super) append_known_profile_kind_count: usize,
    pub(super) append_observable_known_profile_count: usize,
    pub(super) append_hidden_state_known_profile_count: usize,
    pub(super) append_scoring_started: bool,
    pub(super) append_score_events_before_update: usize,
    pub(super) append_score_candidate_events: usize,
    pub(super) append_observable_score_candidate_events: usize,
    pub(super) append_hidden_state_score_candidate_events: usize,
    pub(super) append_unknown_profile_score_candidate_events: usize,
    pub(super) append_verifier_required_events: usize,
    pub(super) append_local_accept_events: usize,
    pub(super) append_unique_cpu_accepts_over_exact_cache: usize,
    pub(super) append_observable_unique_cpu_accepts_over_exact_cache: usize,
    pub(super) append_hidden_state_unique_cpu_accepts_over_exact_cache: usize,
    pub(super) append_unknown_profile_unique_cpu_accepts_over_exact_cache: usize,
    pub(super) append_profile_attribution_overlap_accepts: usize,
    pub(super) append_observable_only_unique_cpu_accepts_over_exact_cache: usize,
    pub(super) append_hidden_state_only_unique_cpu_accepts_over_exact_cache: usize,
    pub(super) append_mixed_profile_unique_cpu_accepts_over_exact_cache: usize,
    pub(super) append_unknown_only_unique_cpu_accepts_over_exact_cache: usize,
    pub(super) append_tokens_saved: u64,
    pub(super) append_observable_tokens_saved: u64,
    pub(super) append_hidden_state_tokens_saved: u64,
    pub(super) append_unknown_profile_tokens_saved: u64,
    pub(super) append_observable_only_tokens_saved: u64,
    pub(super) append_hidden_state_only_tokens_saved: u64,
    pub(super) append_mixed_profile_tokens_saved: u64,
    pub(super) append_unknown_only_tokens_saved: u64,
    pub(super) append_observable_only_cost_saved_microusd: u64,
    pub(super) append_hidden_state_only_cost_saved_microusd: u64,
    pub(super) append_mixed_profile_cost_saved_microusd: u64,
    pub(super) append_unknown_only_cost_saved_microusd: u64,
    pub(super) append_cost_saved_microusd: u64,
    pub(super) append_exact_cache_calls_saved_milli_over_parsed_rows: u64,
    pub(super) append_exact_cache_tokens_saved_milli_over_total: u64,
    pub(super) append_exact_cache_cost_saved_milli_over_total: u64,
    pub(super) append_active_clean_calls_saved_milli_over_parsed_rows: u64,
    pub(super) append_active_clean_tokens_saved_milli_over_total: u64,
    pub(super) append_active_clean_cost_saved_milli_over_total: u64,
    pub(super) append_combined_calls_saved_milli_over_parsed_rows: u64,
    pub(super) append_combined_tokens_saved_milli_over_total: u64,
    pub(super) append_combined_cost_saved_milli_over_total: u64,
    pub(super) append_hidden_state_only_calls_saved_milli_over_parsed_rows: u64,
    pub(super) append_hidden_state_only_tokens_saved_milli_over_total: u64,
    pub(super) append_hidden_state_only_cost_saved_milli_over_total: u64,
    pub(super) append_cost_estimate_used: bool,
    pub(super) append_estimated_cost_events: usize,
    pub(super) append_estimated_total_cost_microusd: u64,
    pub(super) append_estimated_cost_saved_microusd: u64,
    pub(super) append_false_accepts: usize,
    pub(super) append_compression_claim_min_rows: usize,
    pub(super) append_compression_claim_allowed: bool,
    pub(super) append_compression_claim_blocker: String,
    pub(super) append_clean_suffix_rows: usize,
    pub(super) append_clean_suffix_score_events: usize,
    pub(super) append_clean_suffix_unique_cpu_accepts_over_exact_cache: usize,
    pub(super) append_clean_suffix_tokens_saved: u64,
    pub(super) append_clean_suffix_cost_saved_microusd: u64,
    pub(super) append_clean_suffix_false_accepts: usize,
    pub(super) append_clean_suffix_local_accept_events: usize,
    pub(super) append_clean_suffix_last_quarantine_row_index: Option<usize>,
    pub(super) append_clean_suffix_claim_allowed: bool,
    pub(super) append_clean_suffix_claim_blocker: String,
    pub(super) stable_decision_log_architecture_key: String,
    pub(super) stable_decision_log_rows: usize,
    pub(super) stable_decision_log_score_candidate_events: usize,
    pub(super) stable_decision_log_unique_cpu_accepts_over_exact_cache: usize,
    pub(super) stable_decision_log_tokens_saved: u64,
    pub(super) stable_decision_log_cost_saved_microusd: u64,
    pub(super) stable_decision_log_false_accepts: usize,
    pub(super) stable_decision_log_local_accept_events: usize,
    pub(super) stable_decision_log_total_tokens: u64,
    pub(super) stable_decision_log_total_cost_microusd: u64,
    pub(super) stable_decision_log_claim_allowed: bool,
    pub(super) stable_decision_log_claim_blocker: String,
    pub(super) stable_decision_log_clean_suffix_rows: usize,
    pub(super) stable_decision_log_clean_suffix_score_candidate_events: usize,
    pub(super) stable_decision_log_clean_suffix_unique_cpu_accepts_over_exact_cache: usize,
    pub(super) stable_decision_log_clean_suffix_tokens_saved: u64,
    pub(super) stable_decision_log_clean_suffix_cost_saved_microusd: u64,
    pub(super) stable_decision_log_clean_suffix_false_accepts: usize,
    pub(super) stable_decision_log_clean_suffix_local_accept_events: usize,
    pub(super) stable_decision_log_clean_suffix_total_tokens: u64,
    pub(super) stable_decision_log_clean_suffix_total_cost_microusd: u64,
    pub(super) stable_decision_log_clean_suffix_last_quarantine_row_index: Option<usize>,
    pub(super) stable_decision_log_clean_suffix_min_rows: usize,
    pub(super) stable_decision_log_clean_suffix_rows_to_min: usize,
    pub(super) stable_decision_log_clean_suffix_claim_allowed: bool,
    pub(super) stable_decision_log_clean_suffix_claim_blocker: String,
    pub(super) stable_clean_token_compression_claim_allowed: bool,
    pub(super) stable_clean_token_compression_claim_blocker: String,
    pub(super) stable_clean_token_compression_unique_cpu_accepts_over_exact_cache: usize,
    pub(super) stable_clean_token_compression_saved_tokens: u64,
    pub(super) stable_clean_token_compression_total_tokens: u64,
    pub(super) stable_clean_token_compression_saved_milli: u64,
    pub(super) stable_clean_token_compression_false_accepts: usize,
    pub(super) stable_serving_cpu_rows: usize,
    pub(super) stable_serving_cpu_score_candidate_events: usize,
    pub(super) stable_serving_cpu_local_accept_events: usize,
    pub(super) stable_serving_cpu_unique_cpu_accepts_over_exact_cache: usize,
    pub(super) stable_serving_cpu_tokens_saved: u64,
    pub(super) stable_serving_cpu_total_tokens: u64,
    pub(super) stable_serving_cpu_false_accepts: usize,
    pub(super) stable_serving_cpu_claim_allowed: bool,
    pub(super) stable_serving_cpu_claim_blocker: String,
    pub(super) stable_serving_cpu_clean_suffix_rows: usize,
    pub(super) stable_serving_cpu_clean_suffix_score_candidate_events: usize,
    pub(super) stable_serving_cpu_clean_suffix_local_accept_events: usize,
    pub(super) stable_serving_cpu_clean_suffix_unique_cpu_accepts_over_exact_cache: usize,
    pub(super) stable_serving_cpu_clean_suffix_tokens_saved: u64,
    pub(super) stable_serving_cpu_clean_suffix_total_tokens: u64,
    pub(super) stable_serving_cpu_clean_suffix_false_accepts: usize,
    pub(super) stable_serving_cpu_clean_suffix_saved_milli: u64,
    pub(super) stable_serving_cpu_clean_suffix_claim_allowed: bool,
    pub(super) stable_serving_cpu_clean_suffix_claim_blocker: String,
    pub(super) append_total_tokens: u64,
    pub(super) append_total_cost_microusd: u64,
    pub(super) append_exact_cache_hits: usize,
    pub(super) append_exact_cache_tokens: u64,
    pub(super) append_exact_cache_cost_microusd: u64,
    pub(super) append_non_exact_rows: usize,
    pub(super) online_bucket_count: usize,
    pub(super) active_bucket_count: usize,
    pub(super) shadow_ready_bucket_count: usize,
    pub(super) candidate_bucket_count: usize,
    pub(super) clean_candidate_profile_ids: Vec<u32>,
    pub(super) clean_candidate_quarantined_profile_ids: Vec<u32>,
    pub(super) clean_candidate_exportable_profile_ids: Vec<u32>,
    pub(super) clean_candidate_reports: Vec<serde_json::Value>,
    pub(super) rejected_bucket_count: usize,
    pub(super) live_route_count: usize,
    pub(super) live_route_bucket_count: usize,
    pub(super) adaptive_refinement_count: usize,
    pub(super) max_bucket_refinement_depth: usize,
    pub(super) product_hot_score_only_registry_path: String,
    pub(super) product_hot_score_only_runtime_source: String,
    pub(super) product_hot_score_only_runtime_loaded: bool,
    pub(super) product_hot_score_only_runtime_active: bool,
    pub(super) product_hot_score_only_active_manifest_disabled: bool,
    pub(super) product_hot_score_only_active_manifest_disable_reason: String,
    pub(super) product_hot_score_only_quarantined: bool,
    pub(super) product_hot_score_only_quarantine_reason: String,
    pub(super) product_hot_score_only_quarantine_false_accepts: usize,
    pub(super) product_hot_score_only_post_quarantine_score_candidate_events: usize,
    pub(super) product_hot_score_only_post_quarantine_false_accepts: usize,
    pub(super) product_hot_phase_trust_filtered_events: usize,
    pub(super) product_hot_score_only_unique_cpu_accepts_over_exact_cache: usize,
    pub(super) product_hot_score_only_tokens_saved: u64,
    pub(super) product_hot_score_only_cost_saved_microusd: u64,
    pub(super) active_clean_calls_saved: usize,
    pub(super) active_clean_tokens_saved: u64,
    pub(super) lost_calls_due_to_quarantine: usize,
    pub(super) lost_tokens_due_to_quarantine: u64,
    pub(super) quarantine_recovery_discovery_events: usize,
    pub(super) quarantine_recovery_discovery_tokens: u64,
    pub(super) quarantine_recovery_auto_subcenter_observe_events: usize,
    pub(super) product_hot_score_only_active_profile_count: usize,
    pub(super) product_hot_score_only_quarantined_profile_count: usize,
    pub(super) product_hot_score_only_quarantined_profile_ids: Vec<u32>,
    pub(super) product_hot_score_only_quarantine_trace_id: String,
    pub(super) product_hot_score_only_quarantine_route_key: String,
    pub(super) product_hot_score_only_quarantine_bucket_key: String,
    pub(super) product_hot_score_only_auto_refinement_candidate_atoms: Vec<String>,
    pub(super) product_hot_score_only_auto_refinement_selected_atoms: Vec<String>,
    pub(super) product_hot_score_only_route_count: usize,
    pub(super) product_hot_score_only_profile_count: usize,
    pub(super) product_hot_score_only_package_bytes: usize,
    pub(super) final_hot_runtime_available: bool,
    pub(super) final_hot_route_ids: Vec<u32>,
    pub(super) final_hot_profile_ids: Vec<u32>,
    pub(super) final_hot_route_count: usize,
    pub(super) final_hot_profile_count: usize,
    pub(super) final_hot_route_profile_edges: usize,
    pub(super) hot_scratch_rebuilds: usize,
    pub(super) hot_scratch_candidate_capacity: usize,
    pub(super) hot_scratch_bytes_estimate: usize,
    pub(super) runtime_budget: PhaseStreamLiveStoreBudgetReport,
    pub(super) product_hot_budget_passed: bool,
    pub(super) warm_miner_budget_passed: bool,
    pub(super) warm_miner_budget_blocker: String,
    pub(super) exact_cache_overlap_excluded: bool,
    pub(super) token_cost_denominator_present: bool,
    pub(super) verifier_binding_bound: bool,
    pub(super) candidate_package_count: usize,
    pub(super) candidate_package_dir: String,
    pub(super) candidate_packages: Vec<PhaseStreamLiveStoreCandidatePackageReport>,
    pub(super) clean_promotion_manifest_path: String,
    pub(super) clean_promotion_package_dir: String,
    pub(super) clean_promotion_manifest_written: bool,
    pub(super) call_token_promotion_manifest_path: String,
    pub(super) call_token_promotion_package_dir: String,
    pub(super) call_token_promotion_manifest_written: bool,
    pub(super) future_shadow: PhaseStreamLiveStoreFutureShadowReport,
    pub(super) future_shadow_route_level_scoring_used: bool,
    pub(super) future_shadow_billing_request_path: String,
    pub(super) future_shadow_billing_request_rows: usize,
    pub(super) future_shadow_billing_request_tokens: u64,
    pub(super) future_shadow_billing_request_current_cost_microusd: u64,
    pub(super) future_shadow_billing_request_ready_for_external_provider_evidence: bool,
    pub(super) provider_evidence_artifacts: LiveStoreProviderEvidenceArtifactsReport,
    pub(super) provider_export_drop_path: String,
    pub(super) provider_export_present: bool,
    pub(super) provider_evidence_chain_report_path: String,
    pub(super) provider_billing_capture_contract_ready: bool,
    pub(super) provider_market_money_claim_allowed: bool,
    pub(super) provider_money_claim_blocker: String,
    pub(super) tail_follow_mode_used: bool,
    pub(super) cold_adapter_json_used: bool,
    pub(super) cold_adapter_strings_used: bool,
    pub(super) timed_lane_json_used: bool,
    pub(super) timed_lane_string_route_used: bool,
    pub(super) timed_lane_btreemap_used: bool,
    pub(super) timed_lane_file_io_used: bool,
    pub(super) direct_mutable_store_used: bool,
    pub(super) score_before_update_used: bool,
    pub(super) quarantine_nwpc_checkpoint_compile_used: bool,
    pub(super) registry_mutation_enabled: bool,
    pub(super) cpu_profile_registry_write_enabled: bool,
    pub(super) serving_profile_artifact_written: bool,
    pub(super) product_promotion_enabled: bool,
    pub(super) local_accept_enabled: bool,
    pub(super) market_money_claim_allowed: bool,
    pub(super) forbidden_flags: ForbiddenFlags,
    pub(super) verdict: &'static str,
    pub(super) blocker: String,
    pub(super) boundary: &'static str,
}

#[derive(Clone, Debug, Serialize)]
pub(super) struct PhaseStreamHotPathDaemonLiveLoopBudgetSmokeReport {
    pub(super) report_kind: &'static str,
    pub(super) mode: &'static str,
    pub(super) input_trace_paths: Vec<String>,
    pub(super) cells: usize,
    pub(super) min_bucket_events: usize,
    pub(super) total_rows: usize,
    pub(super) parsed_rows: usize,
    pub(super) skipped_no_verifier_label: usize,
    pub(super) skipped_no_safe_atoms: usize,
    pub(super) online_bucket_count: usize,
    pub(super) active_bucket_count: usize,
    pub(super) shadow_ready_bucket_count: usize,
    pub(super) candidate_bucket_count: usize,
    pub(super) rejected_bucket_count: usize,
    pub(super) live_route_count: usize,
    pub(super) live_route_bucket_count: usize,
    pub(super) scored_events_before_update: usize,
    pub(super) local_operator_shadow_decisions: usize,
    pub(super) unique_cpu_accepts_over_exact_cache: usize,
    pub(super) tokens_saved: u64,
    pub(super) cost_saved_microusd: u64,
    pub(super) false_accepts: usize,
    pub(super) learning_false_accepts_before_rejection: usize,
    pub(super) current_hot_view_false_label_events: usize,
    pub(super) hot_runtime_available: bool,
    pub(super) hot_route_ids: Vec<u32>,
    pub(super) hot_profile_ids: Vec<u32>,
    pub(super) hot_route_count: usize,
    pub(super) hot_profile_count: usize,
    pub(super) hot_route_profile_edges: usize,
    pub(super) direct_live_hot: PhaseStreamLiveStoreDirectHotReport,
    pub(super) runtime_budget: PhaseStreamLiveStoreBudgetReport,
    pub(super) budget_snapshot_live: bool,
    pub(super) direct_mutable_store_used: bool,
    pub(super) score_before_update_used: bool,
    pub(super) core_numeric_route_bucket_ids_used: bool,
    pub(super) cold_adapter_json_used: bool,
    pub(super) cold_adapter_strings_used: bool,
    pub(super) quarantine_nwpc_checkpoint_compile_used: bool,
    pub(super) package_roundtrip_used: bool,
    pub(super) registry_mutation_enabled: bool,
    pub(super) cpu_profile_registry_write_enabled: bool,
    pub(super) serving_profile_artifact_written: bool,
    pub(super) product_promotion_enabled: bool,
    pub(super) local_accept_enabled: bool,
    pub(super) market_money_claim_allowed: bool,
    pub(super) forbidden_flags: ForbiddenFlags,
    pub(super) verdict: &'static str,
    pub(super) blocker: String,
    pub(super) boundary: &'static str,
}

#[derive(Clone, Debug, Serialize)]
pub(super) struct PhaseStreamHotPathDaemonAppendLiveLoopSmokeReport {
    pub(super) report_kind: &'static str,
    pub(super) architecture_versions: PhaseStreamArchitectureVersionsReport,
    pub(super) mode: &'static str,
    pub(super) decision_log_path: String,
    pub(super) watermark_trace_paths: Vec<String>,
    pub(super) append_trace_paths: Vec<String>,
    pub(super) cells: usize,
    pub(super) min_bucket_events: usize,
    pub(super) watermark_total_rows: usize,
    pub(super) watermark_parsed_rows: usize,
    pub(super) watermark_skipped_no_verifier_label: usize,
    pub(super) watermark_skipped_no_safe_atoms: usize,
    pub(super) append_total_rows: usize,
    pub(super) append_parsed_rows: usize,
    pub(super) append_skipped_no_verifier_label: usize,
    pub(super) append_skipped_no_safe_atoms: usize,
    pub(super) append_hot_view_available_events: usize,
    pub(super) append_route_index_missing_events: usize,
    pub(super) append_score_events_before_update: usize,
    pub(super) append_score_candidate_events: usize,
    pub(super) append_observable_score_candidate_events: usize,
    pub(super) append_hidden_state_score_candidate_events: usize,
    pub(super) append_unknown_profile_score_candidate_events: usize,
    pub(super) append_verifier_required_events: usize,
    pub(super) append_local_accept_events: usize,
    pub(super) append_unique_cpu_accepts_over_exact_cache: usize,
    pub(super) append_observable_unique_cpu_accepts_over_exact_cache: usize,
    pub(super) append_hidden_state_unique_cpu_accepts_over_exact_cache: usize,
    pub(super) append_unknown_profile_unique_cpu_accepts_over_exact_cache: usize,
    pub(super) append_profile_attribution_overlap_accepts: usize,
    pub(super) append_observable_only_unique_cpu_accepts_over_exact_cache: usize,
    pub(super) append_hidden_state_only_unique_cpu_accepts_over_exact_cache: usize,
    pub(super) append_mixed_profile_unique_cpu_accepts_over_exact_cache: usize,
    pub(super) append_unknown_only_unique_cpu_accepts_over_exact_cache: usize,
    pub(super) append_tokens_saved: u64,
    pub(super) append_observable_tokens_saved: u64,
    pub(super) append_hidden_state_tokens_saved: u64,
    pub(super) append_unknown_profile_tokens_saved: u64,
    pub(super) append_observable_only_tokens_saved: u64,
    pub(super) append_hidden_state_only_tokens_saved: u64,
    pub(super) append_mixed_profile_tokens_saved: u64,
    pub(super) append_unknown_only_tokens_saved: u64,
    pub(super) append_observable_only_cost_saved_microusd: u64,
    pub(super) append_hidden_state_only_cost_saved_microusd: u64,
    pub(super) append_mixed_profile_cost_saved_microusd: u64,
    pub(super) append_unknown_only_cost_saved_microusd: u64,
    pub(super) append_cost_saved_microusd: u64,
    pub(super) append_exact_cache_calls_saved_milli_over_parsed_rows: u64,
    pub(super) append_exact_cache_tokens_saved_milli_over_total: u64,
    pub(super) append_exact_cache_cost_saved_milli_over_total: u64,
    pub(super) append_active_clean_calls_saved_milli_over_parsed_rows: u64,
    pub(super) append_active_clean_tokens_saved_milli_over_total: u64,
    pub(super) append_active_clean_cost_saved_milli_over_total: u64,
    pub(super) append_combined_calls_saved_milli_over_parsed_rows: u64,
    pub(super) append_combined_tokens_saved_milli_over_total: u64,
    pub(super) append_combined_cost_saved_milli_over_total: u64,
    pub(super) append_hidden_state_only_calls_saved_milli_over_parsed_rows: u64,
    pub(super) append_hidden_state_only_tokens_saved_milli_over_total: u64,
    pub(super) append_hidden_state_only_cost_saved_milli_over_total: u64,
    pub(super) append_cost_estimate_used: bool,
    pub(super) append_estimated_cost_events: usize,
    pub(super) append_estimated_total_cost_microusd: u64,
    pub(super) append_estimated_cost_saved_microusd: u64,
    pub(super) append_false_accepts: usize,
    pub(super) append_compression_claim_min_rows: usize,
    pub(super) append_compression_claim_allowed: bool,
    pub(super) append_compression_claim_blocker: String,
    pub(super) append_total_tokens: u64,
    pub(super) append_total_cost_microusd: u64,
    pub(super) append_exact_cache_hits: usize,
    pub(super) append_exact_cache_tokens: u64,
    pub(super) append_exact_cache_cost_microusd: u64,
    pub(super) append_non_exact_rows: usize,
    pub(super) online_bucket_count: usize,
    pub(super) active_bucket_count: usize,
    pub(super) shadow_ready_bucket_count: usize,
    pub(super) candidate_bucket_count: usize,
    pub(super) rejected_bucket_count: usize,
    pub(super) live_route_count: usize,
    pub(super) live_route_bucket_count: usize,
    pub(super) learning_false_accepts_before_rejection: usize,
    pub(super) final_hot_runtime_available: bool,
    pub(super) final_hot_route_ids: Vec<u32>,
    pub(super) final_hot_profile_ids: Vec<u32>,
    pub(super) final_hot_route_count: usize,
    pub(super) final_hot_profile_count: usize,
    pub(super) final_hot_route_profile_edges: usize,
    pub(super) runtime_budget: PhaseStreamLiveStoreBudgetReport,
    pub(super) promotion_evidence_eligible: bool,
    pub(super) promotion_evidence_blocker: &'static str,
    pub(super) admission_attempts: usize,
    pub(super) admission_admitted: usize,
    pub(super) admission_rejected: usize,
    pub(super) admission_hot_route_count: usize,
    pub(super) admission_hot_profile_count: usize,
    pub(super) admission_hot_route_profile_edges: usize,
    pub(super) admission_hot_bytes_estimate: usize,
    pub(super) admission_budget_passed: bool,
    pub(super) admission_queue_shadow_only: bool,
    pub(super) exact_cache_overlap_excluded: bool,
    pub(super) token_cost_denominator_present: bool,
    pub(super) verifier_binding_bound: bool,
    pub(super) budget_snapshot_live: bool,
    pub(super) direct_mutable_store_used: bool,
    pub(super) score_before_update_used: bool,
    pub(super) current_hot_view_refreshed_during_append: bool,
    pub(super) core_numeric_route_bucket_ids_used: bool,
    pub(super) cold_adapter_json_used: bool,
    pub(super) cold_adapter_strings_used: bool,
    pub(super) quarantine_nwpc_checkpoint_compile_used: bool,
    pub(super) package_roundtrip_used: bool,
    pub(super) registry_mutation_enabled: bool,
    pub(super) cpu_profile_registry_write_enabled: bool,
    pub(super) serving_profile_artifact_written: bool,
    pub(super) product_promotion_enabled: bool,
    pub(super) local_accept_enabled: bool,
    pub(super) market_money_claim_allowed: bool,
    pub(super) forbidden_flags: ForbiddenFlags,
    pub(super) verdict: &'static str,
    pub(super) blocker: String,
    pub(super) boundary: &'static str,
}

#[derive(Clone, Debug, Serialize)]
pub(super) struct PhaseStreamLiveStoreAdapterSmokeReport {
    pub(super) report_kind: &'static str,
    pub(super) mode: &'static str,
    pub(super) input_trace_paths: Vec<String>,
    pub(super) cells: usize,
    pub(super) min_bucket_events: usize,
    pub(super) total_rows: usize,
    pub(super) parsed_rows: usize,
    pub(super) skipped_no_verifier_label: usize,
    pub(super) skipped_no_safe_atoms: usize,
    pub(super) bucket_policy_kind: &'static str,
    pub(super) adaptive_refinement_count: usize,
    pub(super) max_bucket_refinement_depth: usize,
    pub(super) synthetic_rows: usize,
    pub(super) non_synthetic_rows: usize,
    pub(super) route_count: usize,
    pub(super) route_bucket_count: usize,
    pub(super) online_bucket_count: usize,
    pub(super) active_bucket_count: usize,
    pub(super) shadow_ready_bucket_count: usize,
    pub(super) candidate_bucket_count: usize,
    pub(super) rejected_bucket_count: usize,
    pub(super) scored_events: usize,
    pub(super) local_operator_shadow_decisions: usize,
    pub(super) unique_cpu_accepts_over_exact_cache: usize,
    pub(super) raw_unique_cpu_accepts_over_exact_cache: usize,
    pub(super) auto_calibrated_unique_cpu_accepts_over_exact_cache: usize,
    pub(super) auto_calibrated_false_accepts: usize,
    pub(super) tokens_saved: u64,
    pub(super) cost_saved_microusd: u64,
    pub(super) false_accepts: usize,
    pub(super) token_cost_denominator_present: bool,
    pub(super) provider_billing_denominator_present: bool,
    pub(super) token_cost_estimate_used: bool,
    pub(super) provider_billing_cost_rows: usize,
    pub(super) estimated_cost_rows: usize,
    pub(super) total_tokens_seen: u64,
    pub(super) total_cost_microusd_seen: u64,
    pub(super) routes: Vec<PhaseStreamLiveStoreRouteReport>,
    pub(super) buckets: Vec<PhaseStreamLiveStoreBucketReport>,
    pub(super) future_shadow: PhaseStreamLiveStoreFutureShadowReport,
    pub(super) verifier_binding_bound: bool,
    pub(super) candidate_package_count: usize,
    pub(super) candidate_package_dir: String,
    pub(super) candidate_packages: Vec<PhaseStreamLiveStoreCandidatePackageReport>,
    pub(super) clean_promotion_manifest_path: String,
    pub(super) clean_promotion_package_dir: String,
    pub(super) call_token_promotion_manifest_path: String,
    pub(super) call_token_promotion_package_dir: String,
    pub(super) runtime_budget: PhaseStreamLiveStoreBudgetReport,
    pub(super) direct_live_hot: PhaseStreamLiveStoreDirectHotReport,
    pub(super) promotion_allowed: bool,
    pub(super) product_metric_allowed: bool,
    pub(super) product_metric_blocker: &'static str,
    pub(super) verdict: &'static str,
    pub(super) next_action: &'static str,
    pub(super) local_accept_enabled: bool,
    pub(super) product_runtime_changed: bool,
    pub(super) serving_runtime_changed: bool,
    pub(super) market_money_claim_allowed: bool,
    pub(super) forbidden_flags: ForbiddenFlags,
    pub(super) boundary: &'static str,
}

#[derive(Clone, Debug, Serialize)]
pub(super) struct PhaseStreamLiveStoreBudgetReport {
    pub(super) max_hot_profiles_per_worker: usize,
    pub(super) max_hot_bytes_per_worker: usize,
    pub(super) max_warm_profiles_per_process: usize,
    pub(super) max_profiles_per_route: usize,
    pub(super) max_route_top_k: usize,
    pub(super) warm_route_count: usize,
    pub(super) warm_profile_count: usize,
    pub(super) warm_metadata_bytes_estimate: usize,
    pub(super) warm_runtime_bytes_estimate: usize,
    pub(super) warm_bytes_estimate: usize,
    pub(super) hot_route_count: usize,
    pub(super) hot_profile_count: usize,
    pub(super) hot_route_profile_edges: usize,
    pub(super) hot_runtime_bytes_estimate: usize,
    pub(super) hot_route_table_bytes_estimate: usize,
    pub(super) hot_bytes_estimate: usize,
    pub(super) hot_budget_passed: bool,
    pub(super) warm_budget_passed: bool,
    pub(super) product_runtime_budget_passed: bool,
}

#[derive(Clone, Debug, Default, Serialize)]
pub(super) struct PhaseStreamLiveStoreDirectHotReport {
    pub(super) bridge_kind: &'static str,
    pub(super) available: bool,
    pub(super) hot_route_count: usize,
    pub(super) hot_profile_count: usize,
    pub(super) hot_route_profile_edges: usize,
    pub(super) hot_bytes_estimate: usize,
    pub(super) product_runtime_budget_passed: bool,
    pub(super) score_events: usize,
    pub(super) route_index_missing_events: usize,
    pub(super) score_candidate_events: usize,
    pub(super) verifier_required_events: usize,
    pub(super) local_accept_events: usize,
    pub(super) unique_cpu_accepts_over_exact_cache: usize,
    pub(super) tokens_saved: u64,
    pub(super) cost_saved_microusd: u64,
    pub(super) score_false_label_events: usize,
    pub(super) local_accept_enabled: bool,
    pub(super) passed: bool,
    pub(super) blocker: &'static str,
}

#[derive(Clone, Debug, Serialize)]
pub(super) struct PhaseStreamLiveStoreRouteReport {
    pub(super) route_id: u32,
    pub(super) route_key: String,
    pub(super) events_seen: usize,
    pub(super) scored_events: usize,
    pub(super) exact_cache_hits: usize,
    pub(super) verified_safe_accepts: usize,
    pub(super) local_operator_shadow_decisions: usize,
    pub(super) unique_cpu_accepts_over_exact_cache: usize,
    pub(super) false_accepts: usize,
    pub(super) tokens_seen: u64,
    pub(super) cost_seen_microusd: u64,
}

#[derive(Clone, Debug, Serialize)]
pub(super) struct PhaseStreamLiveStoreBucketReport {
    pub(super) route_id: u32,
    pub(super) bucket_id: u32,
    pub(super) route_key: String,
    pub(super) bucket_key: String,
    pub(super) events_seen: usize,
    pub(super) scored_events: usize,
    pub(super) exact_cache_hits: usize,
    pub(super) verified_safe_accepts: usize,
    pub(super) local_operator_shadow_decisions: usize,
    pub(super) unique_cpu_accepts_over_exact_cache: usize,
    pub(super) false_accepts: usize,
    pub(super) rejected_by_shadow_safety: bool,
    pub(super) product_candidate_allowed: bool,
    pub(super) tokens_seen: u64,
    pub(super) cost_seen_microusd: u64,
    pub(super) bucket_refinement_depth: usize,
    pub(super) selected_bucket_atoms: Vec<String>,
}

#[derive(Clone, Debug, Serialize)]
pub(super) struct PhaseStreamLiveStoreCandidatePackageReport {
    pub(super) route_id: u32,
    pub(super) bucket_id: u32,
    pub(super) threshold_micro: i64,
    pub(super) package_path: String,
    pub(super) package_bytes: usize,
    pub(super) package_fingerprint64: u64,
    pub(super) record_count: usize,
    pub(super) verifier_bound: bool,
    pub(super) promotion_allowed: bool,
}

#[derive(Clone, Debug, Serialize)]
pub(super) struct PhaseStreamLiveStoreCleanPromotionManifest {
    pub(super) report_kind: &'static str,
    pub(super) manifest_kind: &'static str,
    pub(super) allowed: bool,
    pub(super) blocker: &'static str,
    pub(super) promoted_candidate_count: usize,
    pub(super) quarantined_candidate_count: usize,
    pub(super) hot_route_count: usize,
    pub(super) hot_profile_count: usize,
    pub(super) hot_route_profile_edges: usize,
    pub(super) hot_bytes_estimate: usize,
    pub(super) unique_cpu_accepts_over_exact_cache: usize,
    pub(super) tokens_saved: u64,
    pub(super) cost_saved_microusd: u64,
    pub(super) false_accepts: usize,
    pub(super) runtime_parity_mismatches: usize,
    pub(super) exact_cache_overlap_excluded: bool,
    pub(super) local_accept_enabled: bool,
    pub(super) market_money_claim_allowed: bool,
    pub(super) routes: Vec<PhaseStreamLiveStoreRegistryRouteReport>,
    pub(super) promoted_packages: Vec<PhaseStreamLiveStorePromotedPackageManifestEntry>,
    pub(super) quarantined_candidates: Vec<PhaseStreamLiveStoreQuarantinedCandidateManifestEntry>,
    pub(super) boundary: &'static str,
}

#[derive(Clone, Debug, Serialize, Deserialize)]
pub(super) struct PhaseStreamLiveStorePromotedPackageManifestEntry {
    pub(super) bucket_id: u32,
    pub(super) route_id: u32,
    pub(super) profile_id: u32,
    pub(super) threshold_micro: i64,
    pub(super) package_path: String,
    pub(super) package_fingerprint64: u64,
    pub(super) unique_cpu_accepts_over_exact_cache: usize,
    pub(super) tokens_saved: u64,
    pub(super) cost_saved_microusd: u64,
}

#[derive(Clone, Debug, Deserialize)]
pub(super) struct PhaseStreamLiveStoreCleanPromotionManifestInput {
    pub(super) allowed: bool,
    pub(super) blocker: String,
    pub(super) false_accepts: usize,
    pub(super) runtime_parity_mismatches: usize,
    pub(super) exact_cache_overlap_excluded: bool,
    pub(super) local_accept_enabled: bool,
    pub(super) market_money_claim_allowed: bool,
    pub(super) routes: Vec<PhaseStreamLiveStoreRegistryRouteReport>,
    pub(super) promoted_packages: Vec<PhaseStreamLiveStorePromotedPackageManifestEntry>,
}

#[derive(Clone, Debug, Serialize)]
pub(super) struct PhaseStreamLiveStoreQuarantinedCandidateManifestEntry {
    pub(super) bucket_id: u32,
    pub(super) route_id: u32,
    pub(super) blocker: &'static str,
    pub(super) false_accepts: usize,
    pub(super) runtime_parity_mismatches: usize,
}

#[derive(Clone, Debug, Default, Serialize, Deserialize)]
pub(super) struct PhaseStreamLiveStoreRegistryRouteReport {
    pub(super) route_id: u32,
    pub(super) route_index: usize,
    pub(super) profile_id: u32,
    pub(super) package_fingerprint64: u64,
}

#[derive(Clone, Debug, Default, Serialize)]
pub(super) struct PhaseStreamLiveStoreFutureShadowCandidateReport {
    pub(super) bucket_id: u32,
    pub(super) route_id: u32,
    pub(super) package_fingerprint64: u64,
    pub(super) scored_events: usize,
    pub(super) score_candidate_events: usize,
    pub(super) runtime_margin_parity_checks: usize,
    pub(super) runtime_margin_parity_mismatches: usize,
    pub(super) runtime_decision_parity_mismatches: usize,
    pub(super) unique_cpu_accepts_over_exact_cache: usize,
    pub(super) false_accepts: usize,
    pub(super) tokens_saved: u64,
    pub(super) cost_saved_microusd: u64,
    pub(super) future_promotable: bool,
    pub(super) blocker: &'static str,
    pub(super) promotion_contract_eligible: bool,
    pub(super) promotion_contract_blocker: &'static str,
    pub(super) registry_admission_attempted: bool,
    pub(super) registry_admitted: bool,
    pub(super) registry_admission_blocker: &'static str,
    pub(super) registry_hot_route_count: usize,
    pub(super) registry_hot_profile_count: usize,
    pub(super) registry_hot_bytes_estimate: usize,
    pub(super) registry_budget_passed: bool,
    pub(super) registry_shadow_score_events: usize,
    pub(super) registry_shadow_score_candidate_events: usize,
    pub(super) registry_shadow_unique_cpu_accepts_over_exact_cache: usize,
    pub(super) registry_shadow_tokens_saved: u64,
    pub(super) registry_shadow_cost_saved_microusd: u64,
    pub(super) registry_shadow_false_accepts: usize,
    pub(super) registry_shadow_margin_parity_mismatches: usize,
    pub(super) registry_shadow_decision_parity_mismatches: usize,
}

#[derive(Clone, Debug, Default, Serialize)]
pub(super) struct PhaseStreamLiveStoreFutureShadowReport {
    pub(super) frozen_candidate_count: usize,
    pub(super) runtime_active_candidate_count: usize,
    pub(super) runtime_quarantined_candidate_count: usize,
    pub(super) runtime_quarantined_candidate_ids: Vec<u32>,
    pub(super) scored_events: usize,
    pub(super) score_candidate_events: usize,
    pub(super) runtime_margin_parity_checks: usize,
    pub(super) runtime_margin_parity_mismatches: usize,
    pub(super) runtime_decision_parity_mismatches: usize,
    pub(super) unique_cpu_accepts_over_exact_cache: usize,
    pub(super) false_accepts: usize,
    pub(super) tokens_saved: u64,
    pub(super) cost_saved_microusd: u64,
    pub(super) promotable_candidate_count: usize,
    pub(super) promotable_unique_cpu_accepts_over_exact_cache: usize,
    pub(super) promotable_tokens_saved: u64,
    pub(super) promotable_cost_saved_microusd: u64,
    pub(super) promotable_calls_saved_milli_over_parsed_rows: u64,
    pub(super) promotable_tokens_saved_milli_over_total: u64,
    pub(super) promotable_cost_saved_milli_over_total: u64,
    pub(super) promotion_contract_eligible_candidate_count: usize,
    pub(super) promotion_contract_unique_cpu_accepts_over_exact_cache: usize,
    pub(super) promotion_contract_tokens_saved: u64,
    pub(super) promotion_contract_cost_saved_microusd: u64,
    pub(super) registry_admission_attempts: usize,
    pub(super) registry_admitted_candidates: usize,
    pub(super) registry_rejected_candidates: usize,
    pub(super) registry_hot_route_count: usize,
    pub(super) registry_hot_profile_count: usize,
    pub(super) registry_hot_bytes_estimate: usize,
    pub(super) registry_budget_passed: bool,
    pub(super) registry_shadow_score_events: usize,
    pub(super) registry_shadow_score_candidate_events: usize,
    pub(super) registry_shadow_unique_cpu_accepts_over_exact_cache: usize,
    pub(super) registry_shadow_tokens_saved: u64,
    pub(super) registry_shadow_cost_saved_microusd: u64,
    pub(super) registry_shadow_false_accepts: usize,
    pub(super) registry_shadow_margin_parity_mismatches: usize,
    pub(super) registry_shadow_decision_parity_mismatches: usize,
    pub(super) shared_registry_admission_attempts: usize,
    pub(super) shared_registry_admitted_candidates: usize,
    pub(super) shared_registry_rejected_candidates: usize,
    pub(super) shared_registry_hot_route_count: usize,
    pub(super) shared_registry_hot_profile_count: usize,
    pub(super) shared_registry_hot_route_profile_edges: usize,
    pub(super) shared_registry_hot_bytes_estimate: usize,
    pub(super) shared_registry_budget_passed: bool,
    pub(super) shared_registry_shadow_score_events: usize,
    pub(super) shared_registry_shadow_score_candidate_events: usize,
    pub(super) shared_registry_shadow_unique_cpu_accepts_over_exact_cache: usize,
    pub(super) shared_registry_shadow_tokens_saved: u64,
    pub(super) shared_registry_shadow_cost_saved_microusd: u64,
    pub(super) shared_registry_shadow_false_accepts: usize,
    pub(super) shared_registry_shadow_margin_parity_mismatches: usize,
    pub(super) shared_registry_shadow_decision_parity_mismatches: usize,
    pub(super) shared_registry_route_manifest: Vec<PhaseStreamLiveStoreRegistryRouteReport>,
    pub(super) serving_policy_kind: &'static str,
    pub(super) serving_policy_manifest_route_count: usize,
    pub(super) serving_policy_score_events: usize,
    pub(super) serving_policy_score_candidate_events: usize,
    pub(super) serving_policy_verifier_required_events: usize,
    pub(super) serving_policy_local_accept_events: usize,
    pub(super) serving_policy_unique_cpu_accepts_over_exact_cache: usize,
    pub(super) serving_policy_tokens_saved: u64,
    pub(super) serving_policy_cost_saved_microusd: u64,
    pub(super) serving_policy_false_accepts: usize,
    pub(super) serving_policy_margin_parity_mismatches: usize,
    pub(super) serving_policy_decision_parity_mismatches: usize,
    pub(super) serving_policy_exact_cache_overlap_excluded: bool,
    pub(super) serving_policy_local_accept_enabled: bool,
    pub(super) serving_policy_passed: bool,
    pub(super) serving_policy_blocker: &'static str,
    pub(super) clean_promotion_manifest_kind: &'static str,
    pub(super) clean_promotion_manifest_allowed: bool,
    pub(super) clean_promotion_manifest_blocker: &'static str,
    pub(super) clean_promotion_manifest_promoted_candidates: usize,
    pub(super) clean_promotion_manifest_quarantined_candidates: usize,
    pub(super) clean_promotion_manifest_hot_route_count: usize,
    pub(super) clean_promotion_manifest_hot_profile_count: usize,
    pub(super) clean_promotion_manifest_hot_route_profile_edges: usize,
    pub(super) clean_promotion_manifest_hot_bytes_estimate: usize,
    pub(super) clean_promotion_manifest_unique_cpu_accepts_over_exact_cache: usize,
    pub(super) clean_promotion_manifest_tokens_saved: u64,
    pub(super) clean_promotion_manifest_cost_saved_microusd: u64,
    pub(super) clean_promotion_manifest_false_accepts: usize,
    pub(super) clean_promotion_manifest_runtime_parity_mismatches: usize,
    pub(super) clean_promotion_manifest_exact_cache_overlap_excluded: bool,
    pub(super) clean_promotion_manifest_local_accept_enabled: bool,
    pub(super) clean_promotion_manifest_market_money_claim_allowed: bool,
    pub(super) clean_promotion_manifest_routes: Vec<PhaseStreamLiveStoreRegistryRouteReport>,
    pub(super) call_token_promotion_manifest_kind: &'static str,
    pub(super) call_token_promotion_manifest_allowed: bool,
    pub(super) call_token_promotion_manifest_blocker: &'static str,
    pub(super) call_token_promotion_manifest_promoted_candidates: usize,
    pub(super) call_token_promotion_manifest_quarantined_candidates: usize,
    pub(super) call_token_promotion_manifest_hot_route_count: usize,
    pub(super) call_token_promotion_manifest_hot_profile_count: usize,
    pub(super) call_token_promotion_manifest_hot_route_profile_edges: usize,
    pub(super) call_token_promotion_manifest_hot_bytes_estimate: usize,
    pub(super) call_token_promotion_manifest_unique_cpu_accepts_over_exact_cache: usize,
    pub(super) call_token_promotion_manifest_tokens_saved: u64,
    pub(super) call_token_promotion_manifest_cost_saved_microusd: u64,
    pub(super) call_token_promotion_manifest_false_accepts: usize,
    pub(super) call_token_promotion_manifest_runtime_parity_mismatches: usize,
    pub(super) call_token_promotion_manifest_exact_cache_overlap_excluded: bool,
    pub(super) call_token_promotion_manifest_local_accept_enabled: bool,
    pub(super) call_token_promotion_manifest_market_money_claim_allowed: bool,
    pub(super) call_token_promotion_manifest_routes: Vec<PhaseStreamLiveStoreRegistryRouteReport>,
    pub(super) promotion_gate_allowed: bool,
    pub(super) blocker: &'static str,
    pub(super) candidates: Vec<PhaseStreamLiveStoreFutureShadowCandidateReport>,
}

#[derive(Clone, Debug, Serialize, Deserialize)]
pub(super) struct PhaseStreamLiveStorePreparedHotPack {
    pub(super) pack_kind: String,
    pub(super) manifest_path: String,
    pub(super) cells: usize,
    pub(super) route_count: usize,
    pub(super) profile_count: usize,
    pub(super) rows: Vec<PhaseStreamLiveStorePreparedHotPackRow>,
    pub(super) boundary: String,
}

#[derive(Clone, Debug, Serialize, Deserialize)]
pub(super) struct PhaseStreamLiveStorePreparedHotPackRow {
    pub(super) route_id: u32,
    pub(super) route_index: usize,
    pub(super) atom_ids: Vec<u64>,
    pub(super) verified_safe_accept: bool,
    pub(super) exact_cache_hit: bool,
    pub(super) tokens: u64,
    pub(super) cost_microusd: u64,
}

#[derive(Clone, Debug, Serialize)]
pub(super) struct PhaseStreamLiveStorePreparedHotPackReport {
    pub(super) report_kind: &'static str,
    pub(super) manifest_path: String,
    pub(super) pack_path: String,
    pub(super) input_trace_paths: Vec<String>,
    pub(super) total_rows: usize,
    pub(super) parsed_rows: usize,
    pub(super) route_index_missing_events: usize,
    pub(super) pack_rows: usize,
    pub(super) cells: usize,
    pub(super) hot_profile_count: usize,
    pub(super) hot_route_count: usize,
    pub(super) hot_route_profile_edges: usize,
    pub(super) hot_bytes_estimate: usize,
    pub(super) pack_contains_strings_in_rows: bool,
    pub(super) hot_loop_json_used: bool,
    pub(super) hot_loop_string_route_used: bool,
    pub(super) hot_loop_btreemap_used: bool,
    pub(super) hot_loop_file_io_used: bool,
    pub(super) atom_score_events: usize,
    pub(super) atom_score_candidate_events: usize,
    pub(super) atom_verifier_required_events: usize,
    pub(super) atom_local_accept_events: usize,
    pub(super) atom_unique_cpu_accepts_over_exact_cache: usize,
    pub(super) atom_tokens_saved: u64,
    pub(super) atom_cost_saved_microusd: u64,
    pub(super) atom_false_accepts: usize,
    pub(super) prepared_score_events: usize,
    pub(super) prepared_score_candidate_events: usize,
    pub(super) prepared_verifier_required_events: usize,
    pub(super) prepared_local_accept_events: usize,
    pub(super) prepared_unique_cpu_accepts_over_exact_cache: usize,
    pub(super) prepared_tokens_saved: u64,
    pub(super) prepared_cost_saved_microusd: u64,
    pub(super) prepared_false_accepts: usize,
    pub(super) atom_prepared_margin_parity_mismatches: usize,
    pub(super) atom_prepared_decision_parity_mismatches: usize,
    pub(super) prepared_p50_score_latency_ns: u128,
    pub(super) prepared_p90_score_latency_ns: u128,
    pub(super) prepared_p99_score_latency_ns: u128,
    pub(super) prepared_max_score_latency_ns: u128,
    pub(super) atom_p50_score_latency_ns: u128,
    pub(super) atom_p90_score_latency_ns: u128,
    pub(super) atom_p99_score_latency_ns: u128,
    pub(super) atom_max_score_latency_ns: u128,
    pub(super) latency_repeats: usize,
    pub(super) local_accept_enabled: bool,
    pub(super) market_money_claim_allowed: bool,
    pub(super) verdict: &'static str,
    pub(super) blocker: &'static str,
    pub(super) forbidden_flags: ForbiddenFlags,
    pub(super) boundary: &'static str,
}

#[derive(Clone, Debug, Serialize)]
pub(super) struct PhaseStreamLiveStoreMemoryHotWorkerReport {
    pub(super) report_kind: &'static str,
    pub(super) manifest_path: String,
    pub(super) input_trace_paths: Vec<String>,
    pub(super) total_rows: usize,
    pub(super) parsed_rows: usize,
    pub(super) route_index_missing_events: usize,
    pub(super) prepared_memory_rows: usize,
    pub(super) cells: usize,
    pub(super) hot_profile_count: usize,
    pub(super) hot_route_count: usize,
    pub(super) hot_route_profile_edges: usize,
    pub(super) hot_bytes_estimate: usize,
    pub(super) worker_kind: &'static str,
    pub(super) source_adapter_json_used: bool,
    pub(super) hot_loop_json_used: bool,
    pub(super) hot_loop_string_route_used: bool,
    pub(super) hot_loop_btreemap_used: bool,
    pub(super) hot_loop_file_io_used: bool,
    pub(super) hot_loop_package_compile_used: bool,
    pub(super) score_events: usize,
    pub(super) score_candidate_events: usize,
    pub(super) verifier_required_events: usize,
    pub(super) local_accept_events: usize,
    pub(super) unique_cpu_accepts_over_exact_cache: usize,
    pub(super) tokens_saved: u64,
    pub(super) cost_saved_microusd: u64,
    pub(super) false_accepts: usize,
    pub(super) p50_score_latency_ns: u128,
    pub(super) p90_score_latency_ns: u128,
    pub(super) p99_score_latency_ns: u128,
    pub(super) max_score_latency_ns: u128,
    pub(super) latency_repeats: usize,
    pub(super) local_accept_enabled: bool,
    pub(super) market_money_claim_allowed: bool,
    pub(super) verdict: &'static str,
    pub(super) blocker: &'static str,
    pub(super) forbidden_flags: ForbiddenFlags,
    pub(super) boundary: &'static str,
}

#[derive(Clone, Debug, Serialize)]
pub(super) struct PhaseStreamLiveStoreSourceAdapterWorkerReport {
    pub(super) report_kind: &'static str,
    pub(super) manifest_path: String,
    pub(super) input_sources: Vec<String>,
    pub(super) total_rows: usize,
    pub(super) parsed_rows: usize,
    pub(super) route_index_missing_events: usize,
    pub(super) cells: usize,
    pub(super) hot_profile_count: usize,
    pub(super) hot_route_count: usize,
    pub(super) hot_route_profile_edges: usize,
    pub(super) hot_bytes_estimate: usize,
    pub(super) worker_warmup_route_scores: usize,
    pub(super) worker_kind: &'static str,
    pub(super) source_adapter_streaming_lines_used: bool,
    pub(super) source_adapter_json_used: bool,
    pub(super) stdin_supported: bool,
    pub(super) hot_score_inside_event_loop: bool,
    pub(super) hot_loop_json_used: bool,
    pub(super) hot_loop_string_route_used: bool,
    pub(super) hot_loop_btreemap_used: bool,
    pub(super) hot_loop_file_io_used: bool,
    pub(super) hot_loop_package_compile_used: bool,
    pub(super) score_events: usize,
    pub(super) score_candidate_events: usize,
    pub(super) verifier_required_events: usize,
    pub(super) local_accept_events: usize,
    pub(super) unique_cpu_accepts_over_exact_cache: usize,
    pub(super) tokens_saved: u64,
    pub(super) cost_saved_microusd: u64,
    pub(super) false_accepts: usize,
    pub(super) p50_score_latency_ns: u128,
    pub(super) p90_score_latency_ns: u128,
    pub(super) p99_score_latency_ns: u128,
    pub(super) max_score_latency_ns: u128,
    pub(super) local_accept_enabled: bool,
    pub(super) market_money_claim_allowed: bool,
    pub(super) verdict: &'static str,
    pub(super) blocker: &'static str,
    pub(super) forbidden_flags: ForbiddenFlags,
    pub(super) boundary: &'static str,
}

#[derive(Clone, Debug, Serialize)]
pub(super) struct PhaseStreamLiveStoreWorkerQueueReport {
    pub(super) report_kind: &'static str,
    pub(super) manifest_path: String,
    pub(super) input_sources: Vec<String>,
    pub(super) total_rows: usize,
    pub(super) parsed_rows: usize,
    pub(super) route_index_missing_events: usize,
    pub(super) cells: usize,
    pub(super) queue_batch_capacity: usize,
    pub(super) queue_flushes: usize,
    pub(super) max_observed_queue_depth: usize,
    pub(super) hot_profile_count: usize,
    pub(super) hot_route_count: usize,
    pub(super) hot_route_profile_edges: usize,
    pub(super) hot_bytes_estimate: usize,
    pub(super) worker_kind: &'static str,
    pub(super) source_adapter_json_used: bool,
    pub(super) bounded_memory_queue_used: bool,
    pub(super) hot_worker_drain_loop_isolated: bool,
    pub(super) hot_loop_json_used: bool,
    pub(super) hot_loop_string_route_used: bool,
    pub(super) hot_loop_btreemap_used: bool,
    pub(super) hot_loop_file_io_used: bool,
    pub(super) hot_loop_package_compile_used: bool,
    pub(super) score_events: usize,
    pub(super) score_candidate_events: usize,
    pub(super) verifier_required_events: usize,
    pub(super) local_accept_events: usize,
    pub(super) unique_cpu_accepts_over_exact_cache: usize,
    pub(super) tokens_saved: u64,
    pub(super) cost_saved_microusd: u64,
    pub(super) false_accepts: usize,
    pub(super) p50_score_latency_ns: u128,
    pub(super) p90_score_latency_ns: u128,
    pub(super) p99_score_latency_ns: u128,
    pub(super) max_score_latency_ns: u128,
    pub(super) local_accept_enabled: bool,
    pub(super) market_money_claim_allowed: bool,
    pub(super) verdict: &'static str,
    pub(super) blocker: &'static str,
    pub(super) forbidden_flags: ForbiddenFlags,
    pub(super) boundary: &'static str,
}

#[derive(Clone, Debug, Serialize)]
pub(super) struct PhaseStreamLiveStoreCleanManifestShadowReport {
    pub(super) report_kind: &'static str,
    pub(super) manifest_path: String,
    pub(super) input_trace_paths: Vec<String>,
    pub(super) manifest_allowed: bool,
    pub(super) manifest_blocker: String,
    pub(super) manifest_false_accepts: usize,
    pub(super) manifest_runtime_parity_mismatches: usize,
    pub(super) manifest_exact_cache_overlap_excluded: bool,
    pub(super) promoted_package_count: usize,
    pub(super) route_count: usize,
    pub(super) loaded_package_count: usize,
    pub(super) loaded_record_count: usize,
    pub(super) cells: usize,
    pub(super) hot_profile_count: usize,
    pub(super) hot_route_count: usize,
    pub(super) hot_route_profile_edges: usize,
    pub(super) hot_bytes_estimate: usize,
    pub(super) route_manifest_index_mismatches: usize,
    pub(super) total_rows: usize,
    pub(super) parsed_rows: usize,
    pub(super) skipped_no_verifier_label: usize,
    pub(super) skipped_no_safe_atoms: usize,
    pub(super) route_index_missing_events: usize,
    pub(super) score_events: usize,
    pub(super) score_candidate_events: usize,
    pub(super) verifier_required_events: usize,
    pub(super) local_accept_events: usize,
    pub(super) unique_cpu_accepts_over_exact_cache: usize,
    pub(super) tokens_saved: u64,
    pub(super) cost_saved_microusd: u64,
    pub(super) false_accepts: usize,
    pub(super) runtime_margin_parity_mismatches: usize,
    pub(super) runtime_decision_parity_mismatches: usize,
    pub(super) exact_cache_overlap_excluded: bool,
    pub(super) p50_score_latency_ns: u128,
    pub(super) p90_score_latency_ns: u128,
    pub(super) p99_score_latency_ns: u128,
    pub(super) max_score_latency_ns: u128,
    pub(super) local_accept_enabled: bool,
    pub(super) market_money_claim_allowed: bool,
    pub(super) product_metric_allowed: bool,
    pub(super) verdict: &'static str,
    pub(super) blocker: &'static str,
    pub(super) forbidden_flags: ForbiddenFlags,
    pub(super) boundary: &'static str,
}

#[derive(Clone, Debug, Serialize)]
pub(super) struct PhaseStreamLiveStoreWorkerThreadReport {
    pub(super) report_kind: &'static str,
    pub(super) manifest_path: String,
    pub(super) input_sources: Vec<String>,
    pub(super) total_rows: usize,
    pub(super) parsed_rows: usize,
    pub(super) route_index_missing_events: usize,
    pub(super) cells: usize,
    pub(super) channel_capacity: usize,
    pub(super) sent_events: usize,
    pub(super) worker_warmup_route_scores: usize,
    pub(super) hot_profile_count: usize,
    pub(super) hot_route_count: usize,
    pub(super) hot_route_profile_edges: usize,
    pub(super) hot_bytes_estimate: usize,
    pub(super) worker_kind: &'static str,
    pub(super) source_adapter_json_used: bool,
    pub(super) bounded_sync_channel_used: bool,
    pub(super) worker_thread_used: bool,
    pub(super) hot_loop_json_used: bool,
    pub(super) hot_loop_string_route_used: bool,
    pub(super) hot_loop_btreemap_used: bool,
    pub(super) hot_loop_file_io_used: bool,
    pub(super) hot_loop_package_compile_used: bool,
    pub(super) score_events: usize,
    pub(super) score_candidate_events: usize,
    pub(super) verifier_required_events: usize,
    pub(super) local_accept_events: usize,
    pub(super) unique_cpu_accepts_over_exact_cache: usize,
    pub(super) tokens_saved: u64,
    pub(super) cost_saved_microusd: u64,
    pub(super) false_accepts: usize,
    pub(super) source_prepare_p50_latency_ns: u128,
    pub(super) source_prepare_p90_latency_ns: u128,
    pub(super) source_prepare_p99_latency_ns: u128,
    pub(super) source_send_p50_latency_ns: u128,
    pub(super) source_send_p90_latency_ns: u128,
    pub(super) source_send_p99_latency_ns: u128,
    pub(super) queue_wait_p50_latency_ns: u128,
    pub(super) queue_wait_p90_latency_ns: u128,
    pub(super) queue_wait_p99_latency_ns: u128,
    pub(super) worker_score_p50_latency_ns: u128,
    pub(super) worker_score_p90_latency_ns: u128,
    pub(super) worker_score_p99_latency_ns: u128,
    pub(super) worker_score_max_latency_ns: u128,
    pub(super) local_accept_enabled: bool,
    pub(super) market_money_claim_allowed: bool,
    pub(super) verdict: &'static str,
    pub(super) blocker: &'static str,
    pub(super) forbidden_flags: ForbiddenFlags,
    pub(super) boundary: &'static str,
}

#[derive(Clone, Debug, Serialize)]
pub(super) struct PhaseStreamLiveStoreWorkerBatchThreadReport {
    pub(super) report_kind: &'static str,
    pub(super) manifest_path: String,
    pub(super) input_sources: Vec<String>,
    pub(super) total_rows: usize,
    pub(super) parsed_rows: usize,
    pub(super) route_index_missing_events: usize,
    pub(super) cells: usize,
    pub(super) channel_capacity: usize,
    pub(super) source_batch_capacity: usize,
    pub(super) sent_events: usize,
    pub(super) sent_batches: usize,
    pub(super) max_sent_batch_len: usize,
    pub(super) received_batches: usize,
    pub(super) max_received_batch_len: usize,
    pub(super) worker_warmup_route_scores: usize,
    pub(super) hot_profile_count: usize,
    pub(super) hot_route_count: usize,
    pub(super) hot_route_profile_edges: usize,
    pub(super) hot_bytes_estimate: usize,
    pub(super) worker_kind: &'static str,
    pub(super) source_adapter_json_used: bool,
    pub(super) bounded_sync_channel_used: bool,
    pub(super) batch_messages_used: bool,
    pub(super) worker_thread_used: bool,
    pub(super) hot_loop_json_used: bool,
    pub(super) hot_loop_string_route_used: bool,
    pub(super) hot_loop_btreemap_used: bool,
    pub(super) hot_loop_file_io_used: bool,
    pub(super) hot_loop_package_compile_used: bool,
    pub(super) score_events: usize,
    pub(super) score_candidate_events: usize,
    pub(super) verifier_required_events: usize,
    pub(super) local_accept_events: usize,
    pub(super) unique_cpu_accepts_over_exact_cache: usize,
    pub(super) tokens_saved: u64,
    pub(super) cost_saved_microusd: u64,
    pub(super) false_accepts: usize,
    pub(super) source_prepare_p50_latency_ns: u128,
    pub(super) source_prepare_p90_latency_ns: u128,
    pub(super) source_prepare_p99_latency_ns: u128,
    pub(super) source_send_p50_latency_ns: u128,
    pub(super) source_send_p90_latency_ns: u128,
    pub(super) source_send_p99_latency_ns: u128,
    pub(super) batch_wait_p50_latency_ns: u128,
    pub(super) batch_wait_p90_latency_ns: u128,
    pub(super) batch_wait_p99_latency_ns: u128,
    pub(super) worker_score_p50_latency_ns: u128,
    pub(super) worker_score_p90_latency_ns: u128,
    pub(super) worker_score_p99_latency_ns: u128,
    pub(super) worker_score_max_latency_ns: u128,
    pub(super) local_accept_enabled: bool,
    pub(super) market_money_claim_allowed: bool,
    pub(super) verdict: &'static str,
    pub(super) blocker: &'static str,
    pub(super) forbidden_flags: ForbiddenFlags,
    pub(super) boundary: &'static str,
}

#[derive(Clone, Debug, Serialize)]
pub(super) struct PhaseStreamLiveStoreDirectWorkerBatchThreadReport {
    pub(super) report_kind: &'static str,
    pub(super) runtime_source: &'static str,
    pub(super) input_trace_paths: Vec<String>,
    pub(super) total_rows: usize,
    pub(super) parsed_rows: usize,
    pub(super) skipped_no_verifier_label: usize,
    pub(super) skipped_no_safe_atoms: usize,
    pub(super) route_index_missing_events: usize,
    pub(super) cells: usize,
    pub(super) min_bucket_events: usize,
    pub(super) channel_capacity: usize,
    pub(super) source_batch_capacity: usize,
    pub(super) sent_events: usize,
    pub(super) sent_batches: usize,
    pub(super) max_sent_batch_len: usize,
    pub(super) received_batches: usize,
    pub(super) max_received_batch_len: usize,
    pub(super) worker_warmup_route_scores: usize,
    pub(super) online_bucket_count: usize,
    pub(super) candidate_bucket_count: usize,
    pub(super) rejected_bucket_count: usize,
    pub(super) direct_hot_snapshot_capacity: usize,
    pub(super) direct_hot_snapshot_captured_count: usize,
    pub(super) direct_hot_snapshot_count: usize,
    pub(super) direct_hot_snapshot_evicted_count: usize,
    pub(super) selected_direct_hot_snapshot_index: usize,
    pub(super) selected_direct_hot_snapshot_frozen_after_parsed_rows: usize,
    pub(super) validation_score_event_target: usize,
    pub(super) selected_validation_score_events: usize,
    pub(super) selected_validation_score_candidate_events: usize,
    pub(super) selected_validation_unique_cpu_accepts_over_exact_cache: usize,
    pub(super) selected_validation_tokens_saved: u64,
    pub(super) selected_validation_cost_saved_microusd: u64,
    pub(super) selected_validation_false_accepts: usize,
    pub(super) future_eval_start_after_parsed_rows: usize,
    pub(super) hot_profile_count: usize,
    pub(super) hot_route_count: usize,
    pub(super) hot_route_profile_edges: usize,
    pub(super) hot_bytes_estimate: usize,
    pub(super) runtime_budget: PhaseStreamLiveStoreBudgetReport,
    pub(super) worker_kind: &'static str,
    pub(super) cold_adapter_json_used: bool,
    pub(super) cold_adapter_strings_used: bool,
    pub(super) direct_mutable_store_export_used: bool,
    pub(super) clean_manifest_loaded: bool,
    pub(super) package_roundtrip_used: bool,
    pub(super) bounded_sync_channel_used: bool,
    pub(super) batch_messages_used: bool,
    pub(super) worker_thread_used: bool,
    pub(super) hot_loop_json_used: bool,
    pub(super) hot_loop_string_route_used: bool,
    pub(super) hot_loop_btreemap_used: bool,
    pub(super) hot_loop_file_io_used: bool,
    pub(super) hot_loop_package_compile_used: bool,
    pub(super) score_events: usize,
    pub(super) score_candidate_events: usize,
    pub(super) verifier_required_events: usize,
    pub(super) local_accept_events: usize,
    pub(super) unique_cpu_accepts_over_exact_cache: usize,
    pub(super) tokens_saved: u64,
    pub(super) cost_saved_microusd: u64,
    pub(super) false_accepts: usize,
    pub(super) source_prepare_p50_latency_ns: u128,
    pub(super) source_prepare_p90_latency_ns: u128,
    pub(super) source_prepare_p99_latency_ns: u128,
    pub(super) source_send_p50_latency_ns: u128,
    pub(super) source_send_p90_latency_ns: u128,
    pub(super) source_send_p99_latency_ns: u128,
    pub(super) batch_wait_p50_latency_ns: u128,
    pub(super) batch_wait_p90_latency_ns: u128,
    pub(super) batch_wait_p99_latency_ns: u128,
    pub(super) worker_score_p50_latency_ns: u128,
    pub(super) worker_score_p90_latency_ns: u128,
    pub(super) worker_score_p99_latency_ns: u128,
    pub(super) worker_score_max_latency_ns: u128,
    pub(super) local_accept_enabled: bool,
    pub(super) market_money_claim_allowed: bool,
    pub(super) verdict: &'static str,
    pub(super) blocker: &'static str,
    pub(super) forbidden_flags: ForbiddenFlags,
    pub(super) boundary: &'static str,
}
