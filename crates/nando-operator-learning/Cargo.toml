[package]
name = "nando-operator-learning"
version.workspace = true
edition.workspace = true
license-file.workspace = true
publish.workspace = true
rust-version.workspace = true

[dependencies]
libc = "0.2"
nando-client-evidence = { path = "../nando-client-evidence" }
nando-core = { path = "../nando-core" }
nando-operator-kernel = { path = "../nando-operator-kernel" }
nando-operator-proof = { path = "../nando-operator-proof" }
serde = { version = "1", features = ["derive"] }
serde_cbor = "0.11"
serde_bytes = "0.11"
serde_json = "1"
sha2 = "0.10"
rustix = { version = "1.1.4", features = ["fs", "process"] }
zstd = "0.13"

[dev-dependencies]
nando-operator-runtime = { path = "../nando-operator-runtime" }

[lints]
workspace = true
