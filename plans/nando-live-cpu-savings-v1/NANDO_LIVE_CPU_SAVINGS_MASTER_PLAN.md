# Nando Live CPU Savings Master Plan

Status: canonical execution plan.

Revision: 2026-08-09.

```text
PLAN_STRUCTURE_PASS        PASS
IMPLEMENTATION_PASS        PARTIAL
AUTONOMOUS LAW #2          NOT PROVED
NATURAL RAW PHASE CAUSAL   NOT PROVED
```

Architectural authority remains `ARCHITECTURE_CANON.md`. This document owns
the ordered product, epistemic, mechanism, accounting, and operating work
required to turn previously unknown natural traffic into independently
verified CPU execution. Status reports are evidence; they do not override this
plan.

## 1. Mission

The finite engineering target is:

```text
previously unseen ordinary natural surface
-> source-neutral pre-action capture
-> operator-blind structural frontier
-> autonomous frozen candidate
-> Raw Phase hypothesis formation inside the frozen domain
-> existing OperatorIdentificationMachineV1
-> independent post-freeze future
-> LawCertificate
-> BundleV4 + external admission
-> typed CPU execution before upstream
-> independent verifier
-> local accept or exact upstream fallback
-> durable avoided-upstream and economics receipt
```

The loop is complete only when an unseen eligible surface can traverse that
route without a code change, handwritten family mapping, manually selected
profile, threshold weakening, or synthetic natural evidence.

The product objective is to maximize real global ordinary input-token savings,
subject to:

```text
unverified local accepts       0
active runtime false accepts   0
runtime parity failures        0
missing terminal receipts      0
```

The scientific north star is:

```text
K1 operators
-> natural L2 composition
-> strategy laws
-> learning operators
-> self-growing verified action language
```

CPU savings finance and feed the scientific loop. They do not replace it.

## 2. Claim Levels

These verdicts are independent and must never be collapsed:

| Verdict | Meaning |
|---|---|
| `PLAN_STRUCTURE_PASS` | The proposed routes and authority boundaries are structurally coherent. No implementation claim. |
| `IMPLEMENTATION_PASS` | Exact code, tests, installed artifacts, runtime state, and receipts prove one scoped route. |
| `CAPABILITY_PASS` | A typed executor or verifier works on fixtures or shadow evidence. Not a natural law. |
| `LAW_PASS` | Natural support, frozen version space, independent future, cleanup, and LawCertificate all pass. |
| `MECHANISM_PASS` | Causal ablations and heldout transfer prove the claimed Raw Phase/Wave mechanism. |
| `PRODUCT_PASS` | An admitted package completed ordinary traffic locally before upstream with an independent verifier. |

The 2026-08-09 `PLAN_STRUCTURE_PASS` is backed by six owner-bounded gates:
end-to-end route, discovery authority, adaptive frontier, product/accounting,
durability/runtime, and claim boundaries. The rejected mixed ten-route
worksheet is not supporting evidence. All six gates are structural-only with
`authority_ready = false`.

No verdict inherits authority from another. In particular:

```text
routing PASS != answer-quality PASS
capability PASS != natural LAW_PASS
LAW_PASS != MECHANISM_PASS
avoided call != provider-billed money saving
dashboard coherence != execution authority
```

## 3. Verified Live Baseline

Snapshot: 2026-08-09 22:00 EEST, mini-PC `e@192.168.3.94`.

### Deployment

```text
deployed source commit       57cfee6f0bc73b076a13abf0a8afa127c3f90116
installed learner SHA-256    962b2e1de1e2a958d0d31de81a97d2534e327b37d831aae61b2f8ae3ccaa4459
deployment receipt root      36339b9b0e47a166162ae8e4b21994c803616a4ff2e19cc1e493bc087a55b7b5
K1 scheduler enabled         true
retired MS3 research         CLOSED
hot PID unchanged            3901227
Nginx PID unchanged          682430
service restarts             0
```

The cold learner passed the preregistered live resource gate:

```text
warmup                        15 seconds to ready
steady RSS                    1,592,600 KiB
peak HWM                      1,997,080 KiB
steady CPU                    0.20% at final sample
CPU <=25% streak              4 consecutive samples
```

This proves the K1 runtime can remain live without the former 5.18 GiB memory
path or sustained empty CPU work. It does not prove Law #2.

### Exact Product Economics

Authoritative rolling snapshot:
`/var/lib/nando-wave/transition/economics-live.json`, schema
`nando.economics-snapshot.v4`.

```text
current epoch input tokens             503,961,442
verified avoided input tokens           42,297,506
current epoch token share                     8.3%
verified local accepts                         185
avoided upstream calls                         185
verification coverage                         100%
active economics false accepts                   0
runtime parity mismatches                        0
missing evidence receipts                        0
unresolved local outcomes                        0
dedupe conflicts                                 0
pipeline drops                                   0
Product M1                                     PASS
provider-billed counterfactual          UNAVAILABLE
```

The dashboard's all-recorded-partitions view was approximately:

```text
lifetime verified CPU tokens       84,812,803 / 6,450,984,071
lifetime token share                              1.31%
```

The natural MS4 package has a separate conditional denominator:

```text
package-matched tokens            671,290,096 / 671,290,096
package CPU accepts                              2,750
```

That `100%` is package-conditional and must never be presented as global
traffic coverage.

The miner/corpus view still exposes a separate historical completed-window
counter of `8` false accepts and `0` parity failures. It is not the active
economics epoch and cannot be silently merged with active runtime safety. Both
denominators remain visible until the historical rows are independently
adjudicated or permanently retained as historical failures.

### K1 And Frontier

```text
K1 state                         waiting_for_evidence
K1 blocker                       no_readiness_pass_candidate
next generation                  78
scheduler ledger revision        157
catalog / retained / ready       746 / 256 / 0
frontier topology rows           49,836
readiness-PASS cohorts           37
schedulable cohorts              0
consequence types                scalar 648
                                 record 77
                                 collection 20
                                 rendered_sequence 1
                                 boolean 0
completed cohorts excluded       36
capacity-excluded cohorts        454
scheduler authority              false
phase mutation                   false
```

`37 readiness-PASS / 0 schedulable` means evidence readiness is not selection
authority. Completed, duplicate, unsafe, or otherwise ineligible candidates
remain excluded. A new generation may freeze only from the queue after every
veto and exact-registry check passes.

Generation 77 terminated honestly:

```text
ACQUISITION_FAIL
-> selected_role_witness_missing
-> no law, no phase update, no package authority
```

K1 remains `1/3` laws, `1/3` semantic laws, and `1/2` role topologies.

## 4. Four Independent Lanes

### Capture Lane

```text
ordinary request before action
-> client intent identity
-> source-neutral role topology
-> grounded relations
-> terminal route/frame receipt
-> immutable archive generation
```

Capture may report missing, censored, ambiguous, or settled evidence. It may
not infer a program, issue a LawCertificate, mutate phase memory, or activate a
package.

### Epistemic Lane

```text
natural Capture V2 rows
-> adaptive structural frontier
-> K1 candidate queue
-> immutable one-generation freeze
-> Raw Phase hypotheses inside the frozen domain
-> exact bounded version space
-> semantic quotient / probe
-> independent future
-> cleanup verifier
-> LawCertificate
```

K1 chooses an evidence domain, not a physical program. Raw Phase may generate
or rank hypotheses only after the domain, support, budget, and watermarks are
frozen. `OperatorIdentificationMachineV1` remains the sole identifier.

### Product Lane

```text
LawCertificate
-> Epistemic Registry
-> BundleV4
-> external admission
-> bounded lease
-> applicability guard
-> typed executor
-> independent verifier
-> local accept or ABSTAIN
```

MS8 may optimize execution among admitted packages. It may not choose what law
to discover or convert an economic opportunity into epistemic authority.

### Mechanism Lane

```text
anonymous verified deltas across independent surfaces
-> RelationWaveMemory / Raw Phase circuit formation
-> exact-memory cleanup
-> causal phase ablations
-> delayed heldout transfer
-> WAVE_CAUSAL PASS | FAIL
```

Mechanism evidence cannot grant product or LawCertificate authority. Current
natural Raw Phase causality remains `NOT PROVED`.

## 5. Adaptive Natural-Surface Frontier

The frontier is not a fixed list of three surfaces and is not a list of
top-level `ResponseOperation` names. It continuously partitions every observed
eligible pre-action row by source-neutral structure:

```text
capture_generation_root
candidate_structural_root
source_neutral_topology_root
semantic_novelty_signature_root
consequence_type
```

Allowed consequence types are:

```text
scalar | record | collection | boolean | rendered_sequence
```

Every observed row must receive one durable disposition:

```text
RETAINED_READINESS_PENDING
READINESS_PASS
COMPLETED_EXCLUDED
DUPLICATE_ACTIVE_IDENTITY
SAFETY_VETO
FIXTURE_OR_CONTROLLED_EXCLUDED
CENSORED_WITH_REASON
CAPACITY_EXCLUDED_WITH_RANK
PERMANENT_ABSTAIN
```

There is no silent `ignored surface` state. Capacity limits may defer a cohort,
but its count, score, root, and exclusion reason remain observable.

Physical programs, `FILTER`, `COUNT`, `BRANCH`, renderer names, function names,
source names, and active package names are forbidden in pre-freeze ranking.
They may appear only as post-cohort diagnostics or identifier output.

Natural Vocabulary Census V1 remains decode-compatible historical evidence,
but its top-level operation grouping has no selection authority. Structural
Frontier V2 and the K1 queue are the only live selection substrate.

## 6. Preregistered K1 Lifecycle

```text
registry root + frontier root + K1 deficit root
-> rank operator-blind cohorts
-> atomic stale-root check
-> freeze one candidate
-> bounded support / version space
-> unique class | probe | abstain | acquisition failure
-> independent future
-> transfer / cleanup / certification
```

The freeze binds at least:

```text
epistemic_registry_revision and root
frontier catalog root
K1 deficit snapshot root
fixture exclusion root
candidate structural root
consequence type
readiness receipt root
scoring tuple
scheduler schema
discovery basis root
budget
support watermark
contract watermark
future minimum sequence
selected timestamp
```

`discovery basis root` commits the source-neutral K0 curriculum, Raw Phase
hypothesis generator, executable circuit builder, verifier semantics, and
bounded search configuration. K0 may contain universal primitives such as typed
selection, comparison, filtering, counting, transformation, branching, and
rendering. It may not contain a cohort-to-program mapping, source name, package
identity, or a law credited to K1.

A duplicate terminal verdict is scoped to:

```text
cohort structural identity
+ active protocol-mode set root
+ discovery basis root
```

The same tuple remains excluded. A new discovery basis reopens the cohort once
without rewriting the historical verdict. An active generation continues under
its frozen basis and may not be silently upgraded.

Ownership is explicit: operator-learning constructs the basis and runs frozen
synthesis; the K1 scheduler ranks only structural cohorts; certification
authority independently checks the basis plus registry/protocol CAS and appends
the freeze; `OperatorIdentificationMachineV1` alone identifies the law. Reports
and dashboards have no authority in this route.

Candidate support is not invalidated by unrelated traffic advancing the global
contract watermark. Readiness is cohort-local: exact capture generation,
immutable evidence manifest, settled and verified rows, independent lineages,
and safety provenance. Continued natural occurrence is tested only after freeze
by `future_min_sequence` and the bounded deadline.

Terminal generation states:

```text
PASS
ABSTAIN
ACQUISITION_FAIL
PROBE_EXHAUSTED
```

`PROBE_PENDING` is non-terminal. Candidate, version space, committed
predictions, watermarks, and remaining budget stay immutable.

Only one frozen generation may be active. A registry change between ranking
and freeze yields `STALE_BEFORE_FREEZE`; it never retroactively rewrites the
old decision.

## 7. Independent Gate Chain

No gate may infer another gate's PASS.

| Gate | Required independent evidence | Failure behavior |
|---|---|---|
| Capture | pre-action topology, grounded bindings, route and terminal roots | censor with exact reason |
| Readiness | settled rows, verified rows, independent lineages, fixture exclusion | remain collecting |
| Freeze | coherent registry/catalog/deficit roots and fsynced contract | stale or fail closed |
| Identification | exact replay, bounded version space, semantic quotient | probe, abstain, or fail |
| Future | prediction durable before outcome and independent terminal join | censor or contradiction |
| Transfer | package candidate, cleanup receipt, LawCertificate | no registry mutation |
| Promotion | BundleV4, external admission, lease, safety ledger | no activation |
| Execution | applicability, typed executor, independent verifier | ABSTAIN and upstream |
| Economics | intent/attempt join, terminal receipt, exact token accounting | claim `UNKNOWN` |

The same evidence row may be referenced by multiple receipts, but a downstream
receipt must bind the upstream root; it may not reconstruct or reinterpret it.

## 8. Intent, Attempt, And Economics Accounting

The reviewer identity split is mandatory:

```text
client_intent_id
  one ordinary user intent and the dedupe identity

provider_attempt_id
  one actual upstream dispatch attempt; null for a completed local accept

local_execution_receipt_root
  one typed package execution

verifier_receipt_root
  independent verdict over that execution
```

One intent has exactly one terminal product disposition:

```text
VERIFIED_LOCAL_ACCEPT
UPSTREAM_FALLBACK(provider_attempt_id)
CENSORED_OR_DROPPED_WITHOUT_ACCEPT
```

Retries may create multiple provider attempts but never multiple intents or
multiple avoided-call credits. A local accept is counted only when:

```text
execution receipt exists
AND verifier PASS exists
AND upstream dispatch fence proves no attempt occurred
AND terminal economics event is durable
```

Always report separate denominators for:

```text
ordinary intents
provider attempts
verified local accepts
avoided upstream calls
verified avoided input tokens
verification coverage
false accepts
parity failures
missing receipts
unresolved outcomes
```

Global CPU share uses all eligible ordinary input tokens in one declared
accounting epoch. Package-matched, miner-window, current-epoch, lifetime, and
provider-billed scopes are never divided into one another.

Provider-billed money savings stay `UNKNOWN` until billing evidence is joined
to exact avoided intent/attempt identities.

## 9. Authority And Leakage Vetoes

Absolute vetoes:

- no synthetic, controlled, teacher-generated, or fixture row as natural proof;
- no terminal outcome or future response in a pre-action prediction;
- no manual family mapping or profile-specific operator assignment;
- no physical-program hint before freeze and identification;
- no threshold weakening to manufacture readiness;
- no local accept without independent verifier coverage;
- any active false accept durably revokes the exact execution identity;
- no package activation from dashboard, census, Raw Phase, or K1 scheduler;
- no phase mutation before the responsible authority receipt;
- no money claim without provider-billed evidence;
- retired MS3 research remains closed while K1 runs independently;
- unknown, ambiguous, unsupported, or unverified cases fall back upstream.

`ABSTAIN` is a correct safety outcome, not a failed optimization.

## 10. Durability, Retention, And Restart

Every report and state transition is content addressed and crash atomic:

```text
canonical bytes
-> temporary file
-> file fsync
-> atomic rename
-> directory fsync
-> immutable root / coherent latest pointer
```

Append-only ledgers and active generations must survive restart byte for byte.
Retention may never delete:

- an active or `PROBE_PENDING` generation;
- a precommitted prediction awaiting settlement;
- support/future rows referenced by a non-terminal root;
- a LawCertificate, cleanup, revocation, admission, execution, or economics
  receipt;
- an externally anchored ledger prefix;
- the active binary/config rollback set and deployment receipt.

Eligible raw provider payloads have bounded privacy retention. Derived
source-neutral commitments may outlive raw payloads; raw session content may
not be retained indefinitely merely because discovery is incomplete.

Cleanup is retryable and idempotent. A terminal event does not mark retention
complete until the durable cleanup receipt exists.

## 11. Runtime And Blast-Radius Budgets

Production K1 budgets:

```text
warmup to ready                    <= 90 seconds
steady cold RSS                   < 2.5 GiB
cold HWM                          < 3.5 GiB
steady CPU                        <= 25% for 4 samples
active false accepts              0
runtime parity failures           0
hot serving PID change            forbidden unless preregistered
Nginx PID change                  forbidden
```

An over-budget rollout automatically restores the previous cold binary and
configuration. Resource throttling is not a substitute for removing repeated
empty work.

Change blast radius is bounded by route:

```text
capture schema change     new generation; old decoder remains readable
scheduler change          no classifier/generator/threshold/authority change
identifier change         independent oracle equivalence required
product change            external admission and rollback receipt required
dashboard change          no execution or epistemic authority
```

Heavy builds, archive scans, and scored experiments run only on the mini-PC.

## 12. Requirement-To-Evidence Matrix

| Claim | Required evidence | Verdict without it |
|---|---|---|
| Scheduler is autonomous | natural archive advance causes queue/freeze without command | NOT PROVED |
| Full frontier is observed | every eligible row has a durable disposition and denominator | NOT PROVED |
| Raw Phase discovered a law | frozen domain, hypotheses, exact quotient, independent future | NOT PROVED |
| Natural law exists | freeze, version space, future, cleanup, LawCertificate | NOT PROVED |
| Local answer is correct | independent verifier receipt | UNKNOWN |
| Upstream was avoided | route fence and absent provider attempt | UNKNOWN |
| Runtime matches law | runtime parity receipt | UNKNOWN |
| Token savings are real | exact intent-level global denominator | UNKNOWN |
| Money was saved | provider billing joined to avoided attempts | UNKNOWN |
| Raw Phase caused transfer | causal ablations and heldout transfer | UNRESOLVED |

## 13. Implementation Ledger

As of the verified baseline:

| Route | Status |
|---|---|
| Retired MS3 decoupled from K1 | `IMPLEMENTATION_PASS` |
| Operator-blind Structural Frontier V2 | `IMPLEMENTATION_PASS` |
| Immutable report/latest publication | `IMPLEMENTATION_PASS` |
| Streaming K1 bootstrap vs full oracle | `IMPLEMENTATION_PASS` |
| K1 restart state and ledger recovery | `IMPLEMENTATION_PASS` |
| K1 memory/CPU production budget | `IMPLEMENTATION_PASS` |
| Exact intent-level token accounting | `IMPLEMENTATION_PASS` |
| Verified CPU before upstream | `PRODUCT_PASS` for admitted packages |
| Product M1: >=100 calls, >=1%, 100% verification | `PASS` in current epoch |
| Full observed frontier disposition coverage | `PARTIAL`; capacity and historical rows require audit |
| Raw Phase as sole natural synthesis path | `PARTIAL`; production causal route not proved |
| Natural Law #2 | `NOT PROVED` |
| K1 vocabulary open | `NO`, 1/3 |
| Natural L2 | `BLOCKED` by K1 |
| Provider-billed money savings | `UNKNOWN` |

## 14. Ordered Delivery

1. Keep the now-live K1 runtime within the resource budget and preserve its
   streaming/full-oracle equivalence on every runtime change.
2. Audit `37 readiness-PASS / 0 schedulable` to a root-complete partition:
   completed, duplicate, safety-vetoed, capacity-deferred, or stale. No row may
   disappear behind an aggregate blocker.
3. Make frontier updates incremental for every consequence type, including the
   currently absent natural boolean surface, without source-specific hints.
4. Bind Raw Phase hypothesis formation to the immutable K1 freeze and existing
   identifier. Prove that no fixture/profile/operation name enters selection.
5. Let the next genuinely novel readiness-PASS cohort traverse identification,
   independent future, cleanup, LawCertificate, BundleV4, admission, and typed
   CPU execution without manual intervention.
6. Measure Law #2's incremental global token coverage, not its package-conditional
   match rate. Preserve zero active false accepts and full verification.
7. Repeat for Law #3. Freeze `K1VocabularySnapshotV1` only at 3 distinct laws,
   3 semantic consequences, 2 topology roots, complete cleanup, and zero active
   false bad apply.
8. Open natural L2 over the frozen K1 vocabulary without supplying a DAG. MS7
   is only the bounded executor; the composition law must be discovered.
9. Continue product portfolio optimization independently, with the next target
   being stable >=10% global current-epoch token savings and then progressively
   higher verified coverage.

## 15. Milestone Gates

```text
M1 PRODUCT SIGNAL
  >=100 avoided calls
  >=1% global current-epoch input-token saving
  100% verifier coverage
  0 active false accepts
  status: PASS at this checkpoint

M2 AUTONOMOUS FRONTIER
  all observed eligible rows durably dispositioned
  no manual profile/family mapping
  one unseen cohort freezes automatically
  status: PARTIAL

M3 LAW #2 PRODUCT TRANSFER
  second natural LawCertificate
  independent semantics or topology
  ordinary verified CPU completion
  incremental global denominator reported
  status: NOT PROVED

M4 K1 OPEN
  3 laws / 3 semantics / 2 topologies
  cleanup complete / false bad apply 0
  stable >=10% global verified token saving
  status: CLOSED

M5 NATURAL L2
  composition discovered from K1 effects
  no supplied DAG or family mapping
  independent future and MetaSkillPackage
  status: BLOCKED
```

Failure to broaden global verified coverage after Law #2 is a product stop
signal. It is not permission to weaken science or safety gates; it means the
research lane should be frozen instead of accumulating more infrastructure.

## 16. Work Efficiency

Optimize accepted result per model/tool transition, not token count per turn:

- batch independent reads, status checks, and test suites;
- filter large JSON/logs to roots, counts, blockers, and errors;
- preserve raw evidence on disk instead of in model context;
- run heavy Rust and archive work only on the mini-PC;
- poll only a returned live session identifier;
- keep generated caches out of search and Git status;
- make one scoped behavior change per commit and transactional deployment;
- do not rebuild or hash a full archive when an exact append cursor can prove
  the same result;
- stop a route when its preregistered hard blocker is reached.

Nando-routed completions prove routing and avoided upstream work only. Without
an independent verifier, answer quality remains `UNKNOWN`.
