# K2 Self-Formed Uncertainty V3 Adversarial Critique

Status: `COMPLETE / REPAIRS REQUIRED BELOW ARE INCORPORATED IN V3`

Date: `2026-08-15`

Authority: `FALSE`

## Verdict

V3 repairs the impossible V2 cardinality without hiding incomplete search. The
scientific question remains finite and falsifiable, but the tournament creates
new shortcut and ownership risks that must be explicitly tested.

## P0 Findings

| ID | Attack | Required closure |
|---|---|---|
| P0-1 | The quotient could discard exact outcomes and make final elimination unverifiable. | Retain every exact raw prediction and post-manifest witness; quotient changes only class identity. |
| P0-2 | A top-eight reduction could quietly replace complete frontier formation. | Retain every quotient class; tournament is over all representatives and no class is dropped. |
| P0-3 | Tournament grouping could change the winner if ranking depends on the candidate set. | Prove every probe evaluation is context-free for fixed four models and compare tournament winner with a direct complete-frontier reconstruction. |
| P0-4 | Final filler probes could reintroduce an already eliminated global winner. | Bind a checked dominance chain from every filler to a still-active probe and reject duplicates. |
| P0-5 | Baselines could be weakened by seeing only tournament requests. | Evaluate baselines and oracle over the complete representative set. |
| P0-6 | Generator could shape representative count or ordering around the private answer. | Public generator bytes contain no models, classes, probe keys, expected winner, topology, or private truth; equivariance controls remain mandatory. |
| P0-7 | Verifier could import the tournament implementation and repeat the same bug. | Final verifier computes the direct winner independently and imports canonical types/hashing only. |

## P1 Findings

| ID | Risk | Required closure |
|---|---|---|
| P1-1 | Pairwise equality bits may not define a valid equivalence relation after tampering. | Validate reflexive closure implicitly and reject nontransitive six-bit matrices. |
| P1-2 | Same partition with different eligibility/accounting could be merged. | Include all frozen eligibility, safety, risk, cost, and baseline bits in the key. |
| P1-3 | Canonical representative selection could depend on input order. | Sort by raw-probe root and require the lexicographically smallest class member. |
| P1-4 | Derived denominators could overflow or drift across restart. | Use checked arithmetic and bind all counts into frontier, tournament, batch, and journal roots. |
| P1-5 | Repeated selector calls could exceed resource limits. | Freeze a maximum of 256 selector requests per case and a 20-minute batch wall limit; resource excess is terminal. |
| P1-6 | A tournament request could reuse or omit an active probe. | Each reduction step binds the exact active-set root before and after, eight unique inputs, one retained winner, and seven eliminated roots. |
| P1-7 | Stable-root behavior could differ between direct and tournament paths. | Final direct comparator includes exact stable probe root and must match final precommit bytes. |
| P1-8 | V2 evidence might be rewritten to conceal the defect. | Preserve V2 bytes and publish the discrepancy, V3, critique, and preflight delta append-only. |

## Strongest Shortcut

The strongest fake is to enumerate all `1,792` probes for a denominator receipt,
then silently pass eight hand-picked probes that include the known useful one.
V3 blocks this only if the journal proves a complete representative membership
root, every tournament step conserves its active set, and the independent direct
winner equals the final selector winner. Those three checks are mandatory and
conjunctive.

## Resource Bound Repair

One reduction removes seven active probes. With at most `1,792`
representatives, fewer than `256` reduction requests are sufficient before the
final request. Freeze:

```text
maximum selector requests per case       256
probes per predecessor request              8
models per predecessor request              4
maximum adapted predictions per case     8,192
```

The actual denominator is lower and derived exactly. The bound grants no right
to truncate a frontier.

## Final Assessment

With P0-1 through P0-7 and P1-1 through P1-8 encoded as tests and receipts, V3
is stronger than V2: it no longer hardcodes a prepared eight-class world and it
still preserves the predecessor scorer bytes. Structural PASS remains
coherence-only and authority remains false.

