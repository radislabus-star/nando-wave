# K2 Hidden Composition Representation Capability Result

Status: `K2_HIDDEN_COMPOSITION_REPRESENTATION_CAPABILITY_PASS`

Date: `2026-08-15`

Authority: `FALSE`

## 1. Result

The preregistered generated experiment passed its complete conjunctive
contract:

```text
six generated training tasks
-> complete exact-search decision labels
-> compact learned hidden representation
-> model freeze before confirm reveal
-> two identity/path/content/topology-disjoint confirm tasks
-> bounded beam search
-> independent full-denominator verification
-> real isolated filesystem execution
-> separate exact oracle
```

The learned policy reached both exact confirmatory goals with `61` action
evaluations per task. The independent baseline accounted for all `8,659`
programs per task. This is `0.704469%` of that frozen search denominator and
is below the preregistered `1.2%` limit.

The valid scoped conclusion is:

```text
generated training decisions
-> learned source-neutral parameters
-> exact transfer to two sealed generated compositions
-> bounded search compression
```

## 2. Exact Denominators

```text
train tasks                                      6 / 6
train complete denominator                      8,659 each
training pairs                                  216 / 216 correctly ranked
nonzero learned parameters                       30
sealed confirm tasks                              2 / 2
confirm root intersections                        0
trainer-byte confirm leakage                      0
confirm complete denominator                    8,659 / 8,659 each
bounded-policy exact goals                        2 / 2
policy action evaluations                        61 / 67 each
minimum satisfying depth                          6 / 6
independent policy verifications                  2 / 2
real sandbox executions                           2 / 2
separate exact oracles                            2 / 2
negative controls                                18 / 18
restart parity                                   every journal prefix
residual generated paths                          0
authority                                         false everywhere
```

The denominator ratio is search accounting, not a wall-clock speedup claim.
An action evaluation and a complete program disposition are different units.

## 3. Causal Evidence

The positive model is not merely a fitted decoration:

```text
positive frozen model exact goals                2 / 2
zero-parameter model exact goals                  0 / 2
each destructive parameter control              <= 1 / 2
opaque action-ID permutation preserved            2 / 2
bijective path rename preserved                    2 / 2
tampered traces, budgets, denominators, replay     rejected
authority promotion                               rejected
```

The trainer received only the frozen source-neutral relation atoms and binary
continuation labels. It did not receive confirm IDs, paths, contents, expected
programs, fixture labels, or complete-baseline outcomes. The model froze before
the confirm seal opened.

## 4. Outcome Roots

```text
model             4410b40c63ede8a37e4aeaf0bdcbfa90fe4f7f1d0e2a3cdb0d5e8a57b1fc526d
confirm seal      6148aef1cc56fe727119ed13cdda292abe89b44a79cfdb62752ccf755e66718d
ablation          22f480f65a3e1b942225b8a4870c217ec3149373393ab3a1467c3b60f9f33b9c
terminal          d06d83309dd05f154db1da19ee0468aee3b31b38b309e7ce5109f2680ce3f2df
journal terminal  ee6c79f4f1af7b733b6980443057268233b97870d67563d0f2d2fe4762f46115
```

Machine receipt:

```text
evidence/K2_HIDDEN_COMPOSITION_REPRESENTATION_CAPABILITY_V1/capability-receipt.json
SHA-256 c5c07cd2990d5f71f935977a932416c7daf6c6ff3b747d9e75243631ddf95a35
```

The decisive PASS log SHA-256 is
`352b5c7f2af55146570621734e206cc4693ad906b814f73f0015d9712c57d89f`.

## 5. Verification

All Rust builds and tests ran on `e-MEGA-MINI-M1-13th` with `-j 20` and the
dedicated target directory
`/home/e/.cache/nando-wave-k2-hidden-representation-target`.

```text
cargo check --all-targets                       PASS
cargo fmt --all -- --check                      PASS
cargo clippy --all-targets -- -D warnings       PASS
nonignored package tests                        450 PASS
ignored real-process regressions                  7 PASS
release decisive representation test              1 PASS
frozen file baselines                            14 PASS
allowed additive module registration              1 exact two-line change
frozen directory modes                            2 PASS
NANDA structural packets                          5 PASS, authority false
observed-source route                             29 nodes / 29 edges / 21 routes
observed-source markers                           58 / 58
```

The implementation stays within the frozen source budgets: eight production
modules, one integration test, five generated-only wrappers, no network calls,
no production mounts, no service changes, and no K1, phase, certificate, or
package mutation.

## 6. Rejected Attempts And Repairs

Two earlier executions are preserved as invalid implementation evidence.
Neither was counted as a scientific attempt with changed fixtures or
thresholds.

Attempt 1 failed because `write_value_matches_goal` compared complete file
entries, including their paths. Equal bytes at two different paths were
therefore called unequal. The frozen relation means value equality, so the
implementation was repaired to compare `(content_sha256, byte_len)` and ignore
path identity. A focused regression now pins this semantic boundary.

Attempt 2 reached both goals but failed control 6. The state ablation masked
only three of the four current-state atoms and therefore did not implement the
preregistered destructive control. The mask was repaired from `[1, 6, 8]` to
`[1, 5, 6, 8]`. No feature, model, fixture, threshold, beam, denominator, or
confirm split changed.

The frozen implementation-preflight inventory also says "add four" generated
process binaries, while the role contract requires and the implementation adds
five distinct wrappers: baseline, trainer, policy, verifier, and worker. This
is a paper-accounting defect. The authoritative preregistration permits up to
six, all five executable roots are distinct, and the mismatch changes neither
the capability result nor authority. It remains recorded rather than silently
rewriting the frozen preflight.

## 7. What The PASS Means

Inside this finite generated filesystem language, Nanda can learn a compact
hidden ranking representation from complete-search training decisions and use
it to guide a much smaller bounded search on two sealed, structurally new
tasks while retaining exact execution and independent verification.

Together with the predecessor results, the proved ladder is now:

```text
hidden-effect induction          PROVED
explicit learned composition     PROVED
hidden representation transfer   PROVED in bounded generated environment
```

## 8. What The PASS Does Not Mean

```text
natural K2                                      NOT PROVED
self-chosen safe inquiry                        NOT PROVED
self-created predicates or action language      NOT PROVED
superiority to every explicit heuristic         NOT PROVED
wall-clock or product speedup                    NOT PROVED
Wave-caused whole-circuit grokking               NOT PROVED
production execution authority                  FALSE
LawCertificate or K1 membership                 FALSE
general intelligence                            NOT CLAIMED
```

Humans supplied the `CopyFile`/`RemoveFile` language and the fourteen relation
atoms. Complete search supplied the training labels. The confirm denominator
contains only two generated tasks. These are material limits, not footnotes.

## 9. Next Scientific Step

The next permitted test is bounded self-chosen safe inquiry:

```text
uncertain learned world model
-> candidate safe probes
-> information-gain ranking under a frozen budget
-> isolated intervention
-> independently observed transition
-> model update only after verified evidence
```

That successor must compare against passive observation and fixed probe
policies, forbid outcome leakage, retain exact rollback, and keep authority
false. Natural K2 remains downstream of independently captured natural goals,
alternatives, decisions, and outcomes.
