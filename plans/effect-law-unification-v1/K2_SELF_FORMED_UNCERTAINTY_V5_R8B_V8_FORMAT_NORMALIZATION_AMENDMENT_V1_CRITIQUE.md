# K2 Self-Formed Uncertainty V5 R8B V8 Format Normalization Amendment V1 Critique

Status: `CRITIQUE COMPLETE / FORMAT-ONLY REPAIR ACCEPTABLE`

Date: `2026-08-22`

## Verdict

No P0 or P1 remains in the repaired amendment. It closes the concrete global
formatter failure without pretending that pre-existing skip directives form a
skip-free complexity measure. The amendment is acceptable for one mechanical
formatting operation followed by the complete non-attempt checkpoint gates.

It does not establish implementation correctness and grants no R8B execution,
publication, deployment, push or scientific authority.

## Adversarial Findings

| Priority | Question | Result |
|---|---|---|
| P1 closed | Can the repair hide formatter failures with new exclusions? | No. Global `cargo fmt --all --check` must pass; adding a skip, ignore or wrapper exclusion is a VETO. |
| P1 closed | Do the larger numbers launder real source growth? | No. They cover only the pinned formatter expansion of already accepted source and are bounded by exact post-format counts. |
| P1 closed | Can formatting silently change authority logic? | The operation is restricted to the exact eight-file formatter output. Existing canonical-byte, route, negative, test and Clippy gates must all pass afterward. |
| P1 closed | Can formatter-version drift change the denominator? | No. The exact stable formatter identity and Rust 2024 edition are pinned. Drift requires a new paper artifact. |
| P2 retained | Eight pre-existing skip directives remain in four measured files. | No new directive is allowed. V8 reports a formatter-clean engineering line budget, not skip-free normalized complexity. |
| P2 retained | Source bytes cease to match the measured compact donor. | Exact before/after hashes are retained; semantic and authority equivalence remain claims of the post-format gates, not of this paper. |

## Required Stop Conditions

Stop before checkpoint freeze if:

```text
the formatter touches a ninth file
any normalized budget fails
any test denominator changes
any canonical-byte or route gate changes
strict Clippy fails
global cargo fmt --all --check remains nonzero
```

## Next Legal Action

Run the structural worksheet. On structural PASS, apply the pinned formatter to
the exact eight measured files and rerun all non-attempt implementation gates.
