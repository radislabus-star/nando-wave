# K2 Self-Formed Uncertainty Preregistration V3

Status: `FROZEN REPAIR ADDENDUM / BEFORE R1 CODE / NO CONFIRM NONCE`

Date: `2026-08-15`

Authority: `FALSE`

Base contract:
`K2_SELF_FORMED_UNCERTAINTY_PREREGISTRATION_V2.md`

Base contract root:
`7875d8809b9340774170d2468b07302e17e503712728173b6efb699f9b768a95`

This addendum changes only the impossible probe quotient, its eight-probe
adapter, and denominators directly implied by that repair. Every other V2
grammar, support, induction, semantic-signature, safety, temporal, authority,
resource, cleanup, negative-control, and one-attempt obligation remains binding.

## 1. Repaired Scientific Route

```text
21 incomplete support transitions
-> complete factorized effect consistency
-> complete semantic world-model quotient
-> complete 256-state x 7-action raw probe enumeration
-> complete scorer-sufficient probe quotient
-> all canonical representatives
-> byte-identical predecessor scorer tournament
-> one isolated probe per case
-> independent observation
-> one surviving private semantic class
```

No prepared model list, prepared probe list, probe role, top-k hint, private
answer, or generator-selected state basis is admitted.

## 2. Raw Evidence Remains Exact

For every raw probe and every induced model, the probe owner still records:

```text
initial exact manifest
opaque action root
transition applied/failure disposition
exact post-manifest
exact observable-outcome root
checked risk and cost
derived applicability/dependency/cleanup bits
authority=false
```

The denominator remains exactly:

```text
raw probes per case                         1,792
raw predictions per case                   7,168
raw probes over sixteen cases             28,672
raw predictions over sixteen cases       114,688
```

Absolute outcomes remain in witness bytes and in final verification. They are
not used as probe-class labels because unchanged initial-state residue is not
observable to the scorer as semantic information gain.

## 3. Scorer-Sufficient Probe Equivalence

For four models in canonical model-root order, derive the six-bit pairwise
equality matrix:

```text
(0=1, 0=2, 0=3, 1=2, 1=3, 2=3)
```

Each bit is true exactly when the two model predictions have the same
observable-outcome root. The complete probe equivalence key is:

```text
pairwise observable-outcome equality matrix
+ eligibility disposition
+ robust safety disposition
+ exact risk units
+ exact cost units
+ applicability hint
+ dependency hint
+ cleanup hint
```

The key is sufficient for every frozen model-guided and rule-baseline
comparison. It is invariant to renaming an outcome root while preserving the
model partition. The smallest canonical raw-probe root represents each class.

Every raw probe belongs to exactly one class. Every class and member root is
emitted. The representative count is derived, not fixed, and must satisfy:

```text
8 <= representatives per case <= 1,792
```

Missing members, extra members, duplicate representatives, a nonminimal
representative, or any quotient field outside the frozen key is terminal FAIL.
No class is discarded, sampled, beam-pruned, or selected by top-k.

## 4. Exact Eight-Request Tournament

The predecessor selector source remains byte-identical at SHA-256:

`733b9b59fdfd7e2b5ed68461da89a27c84f04ade2e4e51ae5243dbb7175ef390`

It still receives only canonical `K2InquirySelectorRequestV1` values containing
exactly four models and eight distinct probes. To cover a complete frontier of
more than eight representatives, the adapter uses this frozen tournament:

1. sort all representative probes by probe root;
2. take the first eight active probes and submit one canonical predecessor
   request;
3. retain its selected probe and move the other seven to an append-only
   eliminated list bound to that precommit;
4. repeat until at most eight active probes remain;
5. if fewer than eight remain, fill the final request with the smallest
   eliminated distinct probes;
6. submit the final exact eight-probe request and publish its winner.

Every request and precommit is retained. A filler can never become the global
winner: it was previously dominated by a retained winner under the same
context-free total order, and that dominance chain is checked to an active
probe. No outcomes exist during the tournament.

The independent pre-execution verifier directly evaluates the frozen ranking
tuple over every complete-frontier representative:

```text
max minimax_eliminated
-> max pair_separation
-> min risk units
-> min cost units
-> stable probe root
```

PASS requires the direct winner and tournament winner to be byte-identical.
Any omitted representative, changed grouping, wrong filler, broken dominance
chain, selector-source drift, or winner mismatch is terminal FAIL.

## 5. Baselines And Oracle

All four rule baselines and the diagnostic single-model baseline are evaluated
against the complete representative set, not only the tournament requests.
Their frozen comparators are unchanged. The exact one-probe oracle also ranges
over the complete representative set after private reveal.

This prevents the tournament interface from weakening a baseline or oracle.

## 6. Repaired Denominators

These V2 fixed denominators are superseded:

```text
eight representatives per case                   SUPERSEDED
128 representatives over sixteen cases           SUPERSEDED
512 adapted predecessor predictions               SUPERSEDED
```

They become exact derived denominators frozen before any dispatch:

```text
representatives per case          exact quotient count in frontier receipt
representatives total             checked sum over sixteen frontier receipts
tournament requests per case      exact deterministic reduction count
adapted predictions               tournament requests * 8 probes * 4 models
```

The all-case batch root binds every derived count. Approximation, truncation,
or a post-outcome count change is terminal FAIL.

## 7. Negative-Control Delta

V2 controls remain exactly `32 / 32`, with these repaired interpretations:

```text
16 omitted raw probe or quotient member detected by denominator/member roots
17 extra non-derived raw probe or quotient member rejected
22 predecessor scorer or tournament-chain mutation detected
```

Add four mandatory implementation controls outside the scientific `32 / 32`:

```text
T1 absolute post-root quotient is rejected as cardinality-incoherent
T2 representative top-k truncation is rejected
T3 tournament omission or duplicate filler is rejected
T4 tournament winner differs from direct complete-frontier winner is rejected
```

## 8. Implementation Slice Delta

```text
R1  canonical schemas include variable complete-frontier and tournament receipts
R4  emits all scorer-sufficient classes and representatives
R5  exact predecessor tournament plus direct-winner parity
R7  includes T1-T4 and non-eight frontier cardinalities
R9  freezes derived representative and adapted-prediction denominators
```

R0, R2, R3, R6, R8, R10, and the one-attempt boundary are unchanged.

## 9. Claim Boundary

The permitted PASS statement changes only from "eight-class probe frontier" to:

> Nanda mechanically formed the complete scorer-sufficient probe frontier from
> all 1,792 raw interventions and used the byte-identical predecessor scorer as
> an exact tournament to choose the globally best safe probe.

This does not prove learned strategy, learned grammar, natural K2, Wave-causal
grokking, production authority, or open-ended world-model induction.

## 10. Freeze Rule

V3 and V2 together are the canonical contract. V3 supersedes only the clauses
named above. A confirm nonce may not exist until V3 critique, structural gates,
implementation preflight delta, R1-R9, and post-freeze review all pass.

