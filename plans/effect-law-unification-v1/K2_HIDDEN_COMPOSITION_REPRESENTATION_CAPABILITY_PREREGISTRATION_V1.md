# K2 Hidden Composition Representation Capability Preregistration V1

Status: `FROZEN AFTER ADVERSARIAL REVIEW / PAPER AUTHORITY ONLY`

Date: `2026-08-15`

## 1. Finite Question

The predecessor established a complete explicit-search baseline:

```text
learn hidden L1 effects
-> enumerate every bounded composition
-> execute the selected composition
-> reach an exact generated goal
```

This successor asks one narrower question:

> Can a compact source-neutral representation learned only from generated
> training decisions guide a tightly bounded search to exact goals on a sealed,
> identity-disjoint confirmatory split while evaluating less than 1.2% of the
> complete program denominator?

The only permitted positive verdict is:

```text
K2_HIDDEN_COMPOSITION_REPRESENTATION_CAPABILITY_PASS
```

This is a bounded generated-fixture capability claim. It is not natural K2,
grounded meaning over natural decisions, Wave-caused grokking, a LawCertificate,
K1 membership, package activation, product economics, or production authority.

## 2. Null And Required New Evidence

Primary null:

```text
the frozen hidden policy cannot preserve exact confirmatory success under its
search budget; complete typed search remains necessary
```

Shortcut nulls:

```text
fixture identity selects the answer
opaque action IDs encode the answer
path names encode the answer
the policy receives a confirmatory sequence or complete-planner output
a hand-written dependency score, not learned parameters, selects the answer
beam tie-breaking happens to select the answer
training fit is presented as transfer
the worker or policy certifies its own result
```

New evidence required:

```text
frozen source-neutral feature language
generated train labels produced by complete exact search
trainer process isolated from confirmatory fixtures
compact learned parameter snapshot frozen before confirm reveal
two identity/path/content/topology-disjoint confirmatory tasks
complete exact baseline denominator on both confirmatory tasks
bounded learned-policy search on the same laws, state, and goal
exact sandbox execution and independent oracle on both tasks
causal parameter ablations under the same search budget
identity and path permutation transfer controls
durable temporal proof that model freeze precedes confirm reveal
```

## 3. Frozen Authority And Process Roles

| Role | May see | Must not see or do |
|---|---|---|
| Fixture owner | private generated train and confirm fixtures | train weights, rank actions, certify |
| Train baseline process | train law sets, states, goals, complete budget | confirm fixtures, model weights |
| Example projector | verified train baseline output and frozen feature schema | invent labels, read confirm fixtures |
| Trainer process | source-neutral feature rows and binary continuation labels | IDs, paths, contents, confirm roots, expected sequences |
| Model store | canonical trainer output | alter parameters after freeze |
| Confirm seal owner | preregistered commitment and model root | expose confirm target before model freeze |
| Complete baseline process | confirm law sets, states, goals, full denominator | hidden-policy output or ranking |
| Hidden policy process | frozen model, confirm law sets, state, goal, beam budget | baseline output, expected sequence, fixture family label |
| Independent policy verifier | model, request, trace, baseline denominator | call policy search implementation |
| Sandbox worker | explicit selected effect sequence and disposable workspace | model, baseline, goal, repository, network |
| Adapter observer | worker workspace after process exit | infer plan or certify model |
| Exact oracle process | frozen goal and adapter-observed terminal manifest | trainer, policy, baseline internals |
| Journal owner | typed roots and state transitions | create scientific evidence |

The trainer, complete baseline, hidden policy, independent verifier, sandbox
worker, exact oracle, and integration driver executable roots are pairwise
distinct. Every authority flag is false.

## 4. Frozen Observable Language

The experiment reuses the predecessor's explicit learned effect language:

```text
CopyFile { source_path, target_path }
RemoveFile { path }
```

Humans still define this language and the following source-neutral relation
atoms. That limitation is part of the claim, not hidden:

```text
BIAS
ACTION_APPLICABLE
ACTION_IS_COPY
ACTION_IS_REMOVE
WRITE_PATH_REQUIRED_BY_GOAL
WRITE_VALUE_MATCHES_GOAL
WRITE_PATH_CURRENTLY_DIFFERS
WRITE_PATH_READ_BY_REMAINING_ACTION
READ_PATH_MISSING_BUT_WRITABLE
REMOVE_PATH_ABSENT_FROM_GOAL
REMOVE_PATH_REQUIRED_BY_GOAL
REMOVE_PATH_READ_BY_REMAINING_ACTION
WRITE_PATH_NUISANCE
REMAINING_DEPTH_FRACTION
```

The feature vector contains signed bounded integers only. It excludes opaque
IDs, path bytes, content bytes, stable hashes, fixture labels, source order,
expected sequences, baseline ranks, and episode identities.

Feature extraction may expose relations; it may not assign a final usefulness
score. There is no hand-written sum, rule ordering, dependency bonus, or goal
distance heuristic in the policy. The final ordering must come from the frozen
learned parameters, with stable hash used only after an exact score tie.

## 5. Frozen Model Family

One model family is allowed:

```text
14 signed relation atoms
-> 8-unit learned hidden layer
-> clipped ReLU
-> learned scalar continuation score
```

Training is deterministic pairwise ranking over each train decision group:

```text
positive continuation action vs negative action
-> fixed epoch count 256
-> fixed learning-rate schedule
-> fixed L2 bound
-> signed 64-bit integer accumulation
-> final weights clipped to preregistered bounds
```

No architecture, feature, epoch, seed, beam, threshold, or control may change
after the first confirmatory outcome is opened. `MeaningPolicySnapshotV1`
binds:

```text
schema and feature-language root
trainer executable root
complete train-corpus root
all train baseline roots
hidden and output weights
training epochs and update count
training pair accuracy
parameter nonzero count and L1 norm
authority = false
model root
```

The model must contain at least 16 nonzero learned parameters and must rank at
least 95% of frozen train positive/negative pairs correctly. Training accuracy
is an implementation gate, never confirmatory evidence.

## 6. Generated Train Split

Exactly six generated train tasks are derived from one preregistered train
commitment. They vary opaque action IDs, role paths, contents, action order,
target topology, and exact goals. The six source-neutral topology families are:

```text
T1  two-copy chain with ephemeral intermediate cleanup
T2  three-copy chain with two cleanups
T3  one intermediate feeding two goal outputs then cleanup
T4  two independent chains plus one cleanup
T5  copy chain plus an applicable goal-conflicting distractor
T6  copy fork plus a premature-remove hazard
```

Each task has exactly seven learned actions and maximum depth six. A complete
external train baseline enumerates every no-repetition program through the
task's exact minimum goal depth. It supplies only verified decision groups to
the example projector:

```text
state and remaining actions at a satisfying-prefix decision
positive = action begins at least one minimum-depth satisfying continuation
negative = every other available action
```

The trainer receives feature vectors, binary labels, decision-group roots, and
the corpus root. It receives no task names, effects, manifests, action IDs,
paths, contents, programs, or expected sequences.

## 7. Sealed Confirmatory Split

Exactly two confirmatory tasks are derived from a different preregistered
commitment. Before model freeze, only that commitment root and generator schema
root exist in the experiment freeze. Derived IDs, paths, contents, law sets,
targets, goals, and expected programs do not enter trainer protocol bytes.

After the model snapshot is durable, the fixture owner reveals:

```text
C1  depth-6 four-copy ephemeral chain plus two ordered cleanups and one distractor
C2  depth-6 forked dependency graph plus cleanup hazards and one distractor
```

Each confirm task has exactly seven actions, exact minimum satisfying depth six,
and one action that must be excluded. Train and confirm splits must have zero
intersection across:

```text
experiment roots
action IDs
path strings
content hashes
manifest roots
goal roots
program roots
decision-group roots
```

The confirm seal receipt also proves that canonical trainer request bytes do
not contain any confirm-derived root, ID, path, content hash, or expected
program root.

## 8. Complete Baseline Denominator

The complete baseline and hidden policy receive identical confirm law sets,
target manifests, exact goals, maximum depth, and no-repetition contract. The
baseline receives no hidden-policy output.

For seven actions and depths one through six, the exact denominator per task is:

```text
P(7,1) + P(7,2) + P(7,3) + P(7,4) + P(7,5) + P(7,6)
= 7 + 42 + 210 + 840 + 2520 + 5040
= 8659 programs
```

The baseline must disposition all `8659 / 8659` programs independently,
identify every exact satisfying minimum-depth program, and freeze its complete
candidate-set root before it can inspect the policy outcome.

## 9. Hidden Policy Budget

The policy uses beam width three and maximum depth six. At every depth it scores
every unused action extension of each surviving prefix, applies the typed
effect, drops inapplicable transitions, and retains the top three prefixes by:

```text
cumulative learned score DESC
program root ASC only on an exact learned-score tie
```

The exact maximum action-evaluation denominator per confirm task is:

```text
7 + 3*6 + 3*5 + 3*4 + 3*3 + 3*2 = 67
```

Required search compression:

```text
action evaluations <= 67
complete programs represented by baseline = 8659
evaluation ratio <= 0.012
budget retries = 0
```

The policy may return `ABSTAIN`. It passes only if one retained depth-six
program reaches the exact goal. It cannot call the complete baseline, enumerate
the complete denominator, read baseline artifacts, or widen the beam.

## 10. Independent Verification And Execution

The verifier independently:

1. validates model and request roots;
2. reconstructs every feature vector with separate source code;
3. recomputes every hidden score and trace ordering;
4. checks the exact action-evaluation count and beam width;
5. independently reconstructs all 8,659 baseline dispositions;
6. proves the selected program belongs to the minimum satisfying baseline set;
7. proves no strict prefix satisfies the goal;
8. checks every authority flag is false.

The verifier may duplicate transition semantics but may not import or call the
policy search or baseline enumeration implementation.

Only after independent verification does the adapter publish a durable
execution dispatch. The separate sandbox worker executes the selected effects
inside a disposable current-work tree. After worker exit, the adapter scans the
filesystem itself. A separate exact-oracle process compares that observed
manifest to the frozen goal. Both confirm tasks must pass.

## 11. Frozen Controls

Exactly eighteen controls are required:

```text
01 zero all learned parameters
02 deterministic train-label permutation
03 hidden-row permutation without corresponding output repair
04 output-weight permutation
05 mask all goal relation atoms
06 mask all current-state relation atoms
07 replace learned model with frozen initialization
08 beam width one stress control
09 inject confirm expected program into trainer request -> schema reject
10 inject complete-baseline output into policy request -> schema reject
11 tamper one policy score -> verifier reject
12 drop one policy expansion -> verifier reject
13 exceed 67 action evaluations -> verifier reject
14 tamper complete denominator -> verifier reject
15 cross-task policy replay -> verifier reject
16 opaque action-ID permutation preserves both positive results
17 bijective path rename preserves both positive results
18 authority-bit promotion -> every consumer rejects
```

Controls 01-07 must collectively establish causal parameter dependence:

```text
positive frozen model exact goals = 2 / 2
each destructive model control exact goals <= 1 / 2
zero-parameter control exact goals = 0 / 2
```

Controls 16-17 must each preserve `2 / 2` exact success under the same budget.
No failed control may be omitted, retried with changed bytes, or reclassified.

## 12. Temporal And Durability Contract

The append-only journal freezes these transitions in order:

```text
EXPERIMENT_FROZEN
TRAIN_SPLIT_FROZEN
TRAIN_BASELINES_FROZEN
TRAIN_CORPUS_FROZEN
MODEL_TRAINING_DISPATCHED
MODEL_FROZEN
CONFIRM_SEAL_OPENED
CONFIRM_SPLIT_FROZEN
CONFIRM_BASELINES_FROZEN
POLICIES_DISPATCHED
POLICIES_FROZEN
INDEPENDENT_VERIFICATIONS_FROZEN
EXECUTION_1_DISPATCHED
EXECUTION_1_OBSERVED
ORACLE_1_FROZEN
EXECUTION_2_DISPATCHED
EXECUTION_2_OBSERVED
ORACLE_2_FROZEN
CONTROLS_FROZEN
TERMINAL_FROZEN
```

Every append uses temp, file fsync, rename, and directory fsync. Restart parity
must hold after every prefix. An indeterminate external dispatch forbids the
same experiment identity from being rerun. Confirm-derived bytes appearing
before `MODEL_FROZEN` is a terminal `TEMPORAL_LEAK_FAIL`.

## 13. PASS Contract

All conditions are conjunctive:

```text
train tasks                                      6 / 6
train complete baselines                         6 / 6
frozen model pair accuracy                       >= 95%
nonzero learned parameters                       >= 16
confirm split/root intersections                 0
confirm trainer-byte leakage                     0
confirm complete denominators                    8659 / 8659 each
positive bounded-policy exact plans              2 / 2
policy action evaluations                        <= 67 each
search ratio                                     <= 0.012 each
minimum satisfying depth                         6 / 6
independent policy verifications                  2 / 2
real sandbox executions                           2 / 2
separate exact oracles                            2 / 2
controls                                         18 / 18
zero-model exact goals                            0 / 2
destructive model controls                        <= 1 / 2 each
ID and path transfer controls                     2 / 2 each
restart parity                                   every prefix
residual generated paths                          0
authority                                         false everywhere
```

Any failed condition yields one frozen negative verdict. Thresholds, data,
features, model family, and beam budget remain unchanged after outcome reveal.

## 14. Claim Boundary

A PASS establishes only:

```text
generated train decisions
-> compact learned source-neutral parameters
-> exact transfer to two sealed generated compositions
-> <= 1.2% of complete-search denominator
-> real isolated execution and exact oracle
```

It does not establish:

```text
natural K2 or natural grounded meaning
self-created observable predicates
self-chosen interventions
open-ended planning
superiority to every explicit heuristic
Wave-caused phase transition
production quality, savings, or execution authority
general intelligence
```

The next allowed scientific step after PASS is bounded self-chosen safe inquiry.
After a negative result, the frozen contract remains evidence and this exact
attempt is not repeated under a new threshold.

## 15. Source And Resource Budgets

```text
new production modules                         <= 8
each new production source file                < 1800 lines
new integration tests                          <= 2 files, each < 2000 lines
new generated-only binaries                    <= 6
network calls                                  0
production mounts                              0
service changes                                0
K1, phase, certificate, package mutations      0
Rust build jobs on mini-PC                      20
```

All predecessor K2, Law Lab, S1C, and `graphify-out/` bytes are preserved.
Dashboard projection is a later read-only observation slice and cannot enter
this experiment or influence its verdict.

## 16. Exact Structural Contract Index

- The trainer protocol excludes identifiers, paths, contents, roots, and fixture labels.
- Train and confirm commitments derive disjoint task identities, paths, contents, and goals.
- The confirm seal proves confirm-derived tokens are absent from trainer request bytes.
- Opaque action-ID permutation must preserve both exact confirm results.
- Bijective path renaming must preserve both exact confirm results.
- Generated evidence grants no natural K2, Wave, certificate, or production authority.
- The model snapshot freezes before any confirm fixture is revealed.
- The confirm baseline freezes without reading hidden-policy output.
- The hidden policy runs only after model and confirm request roots are durable.
- Every execution dispatch follows independent policy verification.
- An indeterminate dispatch forbids a same-identity rerun.
- Restart projection must reproduce every durable journal prefix.
- The relation extractor emits atoms but no final action usefulness score.
- Learned parameters exclusively determine ranking before exact-score ties.
- A zero-parameter model must reach zero exact confirm goals.
- Every destructive parameter control must reduce success to at most one confirm goal.
- The positive frozen model must transfer to both sealed confirm tasks.
- Training accuracy cannot substitute for confirmatory execution.
- The complete baseline dispositions exactly 8,659 programs for each confirm task.
- The hidden policy evaluates at most 67 action extensions for each confirm task.
- The hidden policy cannot call the complete baseline or read its output.
- The independent verifier reconstructs the denominator and policy trace.
- The selected policy program must belong to the minimum satisfying baseline set.
- Search ratio denotes denominator compression, not wall-clock speedup.
- The verifier cannot call policy or baseline transition implementations.
- Verified selected effects become a durable execution dispatch.
- The sandbox worker mutates only a disposable current-work tree.
- The adapter independently scans the terminal filesystem after worker exit.
- The oracle compares only the frozen goal and adapter-observed manifest.
- Every generated experiment process retains authority false.
