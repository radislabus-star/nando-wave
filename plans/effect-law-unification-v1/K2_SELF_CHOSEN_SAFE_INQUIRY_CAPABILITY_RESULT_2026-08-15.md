# K2 Self-Chosen Safe Inquiry Capability Result

Status: `K2_MODEL_GUIDED_ACTIVE_INQUIRY_PASS`

Date: `2026-08-15`

Authority: `FALSE`

## 1. Result

The single preregistered generated experiment passed its full conjunctive
contract:

```text
four answer-blind competing world models
+ eight opaque candidate probes
-> complete model-predicted outcome matrix
-> exact safety and observability vetoes
-> immutable selection and 32 predictions per case
-> independent pre-execution selection verification
-> one real reversible probe in a disposable sandbox
-> separate read-only observer process
-> independent model elimination and baseline reconstruction
-> one surviving model in every case
```

Across eight sealed cases, the model-guided selector chose the uniquely most
discriminating safe probe without receiving the private true-choice relation or
any post-outcome bytes. It matched the one-probe oracle in `8 / 8` cases and
strictly reduced uncertainty relative to every frozen non-oracle policy.

The permitted conclusion is narrow but real:

```text
prepared answer-blind causal hypotheses
-> predicted consequence partitions
-> one self-chosen safe generated intervention
-> new independently observed evidence
-> exact uncertainty reduction
```

The ranking rule itself is a fixed K0 information-gain primitive. This result
does not claim that Nanda learned that rule, invented the world models, or
created the probe language.

## 2. Exact Denominators

```text
sealed cases                                      8 / 8
public model hypotheses                          32
candidate probes                                 64
eligible / ineligible dispositions            32 / 32
complete model-probe predictions               256 / 256
selection precommits                              8 / 8
independent selection verifications               8 / 8
real isolated probe executions                    8 / 8
separate observer receipts                        8 / 8
singleton updated model sets                      8 / 8
oracle matches                                    8 / 8
negative controls                               18 / 18
generated-provenance control                      PASS
forbidden probe executions                           0
extra best-score ties                                0
journal events                                      10
restart parity                       every legal prefix
residual generated paths                             0
scientific executions                                1
```

The machine field `exact_best_ties=8` is the sum of eight best-set
cardinalities, each of which includes the selected probe itself. It therefore
means one unique best probe per case, not eight unresolved ties.

## 3. Baselines And Costs

The eight cases are identity, path, content, and order disjoint but preserve
the same frozen partition geometry. Per-case values are therefore identical;
the aggregate totals are the values emitted by the sealed receipt.

| Policy | Survivors each | Survivors total | Risk each / total | Cost each / total |
|---|---:|---:|---:|---:|
| Model-guided | 1 | 8 | 2 / 16 | 5 / 40 |
| Passive | 4 | 32 | 0 / 0 | 0 / 0 |
| Stable hash | 2 | 16 | 3 / 24 | 6 / 48 |
| Cheapest first | 4 | 32 | 1 / 8 | 1 / 8 |
| Explicit heuristic | 2 | 16 | 2 / 16 | 4 / 32 |
| Oracle | 1 | 8 | 2 / 16 | 5 / 40 |

The guided route does not claim minimum monetary cost. It claims the exact
preregistered objective: unique identification under one probe while staying
inside the risk and cost ceilings. The cheap probe preserves all four models;
the stronger fixed heuristic and stable-hash decoy preserve two.

## 4. Safety Accounting

Each case contained four eligible probes and four explicit vetoes:

```text
non-reversible high-information probe       REJECTED
ambiguous observation probe                 REJECTED
delayed observation probe                   REJECTED
unknown-effect action probe                 REJECTED
foreign generated-provenance binding        REJECTED in focused control
```

Selection, complete prediction bytes, and independent verification were
durably journaled before the dispatch owner received the private true choice.
The worker and observer were separate executables. The worker mutated only the
disposable tree; the observer received no models, effects, predictions, or
worker stdout and scanned the post-state read-only after worker exit.

## 5. Outcome Evidence

```text
sealed receipt root
739fe66b11e8035815db08e4bec136857851df41cceee6dd24445995eb64f578

terminal event root
c5f25afa92e8a3313743ec2c464610899f505388a1c5697e41727123118a3441

sealed log SHA-256
3f62f032d5b498beda893fa15ee3b67bffd9f6f17db7283964b20542d5459612
```

Machine receipt:

```text
evidence/K2_SELF_CHOSEN_SAFE_INQUIRY_V1/capability-receipt.json
SHA-256 983df88b2d3b3eb822e110aabcfa3d731dcb2889f23590c5c297516bf3723acb
```

The five selector, baseline, verifier, worker, and observer executable roots
are pairwise distinct and are bound in that receipt.

## 6. Verification

All heavy Rust compilation and tests ran on `e-MEGA-MINI-M1-13th` with 20
build jobs and the dedicated target directory
`/home/e/.cache/nando-wave-k2-active-inquiry-target`.

```text
cargo check --all-targets                       PASS
cargo fmt --all -- --check                      PASS
cargo clippy --all-targets -- -D warnings       PASS
nonignored package tests                        451 PASS
development active-inquiry test                   1 PASS
sealed decisive test                              1 PASS
frozen source baselines                          18 exact PASS
allowed module registration                       2 additive lines
preregistration route packets                      5 PASS, authority false
true-choice boundary repair                  NANDA PASS, authority false
observed-source route       18 nodes / 22 edges / 17 routes / 40 of 40 PASS
selector-to-verifier implementation imports        0
forbidden production/network source hits            0
```

The observed-source route keeps execution, authority, observation, and proof
owners separate. Its first documentary run was correctly VETO because edge
evidence was missing; all 22 edges were then bound to exact source lines and
the final gate passed without changing experiment code or result bytes.

No deployment, service control, natural traffic, K1 registry mutation, phase
memory mutation, package activation, LawCertificate, or dashboard change was
performed.

## 7. Preserved Invalid Work

Four pre-scientific defects remain reachable through Git evidence tags:

```text
invalid implementation 01  initial Rust borrow-check failure
invalid design 01          impossible public-root byte-absence rule
invalid implementation 02  1296-byte verifier command rejected by Clippy
invalid implementation 03  incomplete size repair left a 440-byte variant
```

All were found before the one sealed execution. None changed the frozen confirm
commitment, case generator, model/probe counts, scoring tuple, budgets,
baselines, thresholds, or PASS rule.

## 8. Meaning And Boundary

This result closes the next generated capability rung:

```text
hidden-effect induction          PROVED
learned sequential composition   PROVED
hidden representation transfer   PROVED in bounded generated environment
model-guided active inquiry       PROVED in bounded generated environment
```

It still does not establish:

```text
Natural K2                                      NOT PROVED
learned general experiment strategy             NOT PROVED
self-created world models or probe catalog      NOT PROVED
self-created state predicates or action language NOT PROVED
partial-observation or delayed-effect inquiry   NOT PROVED
production execution authority                  FALSE
LawCertificate or K1 registry membership        FALSE
Wave-caused whole-circuit grokking               NOT PROVED
general intelligence                            NOT CLAIMED
```

The post-result critique records the remaining prepared-answer surfaces and
the narrowest justified successor. No successor result is implied by this
PASS.
