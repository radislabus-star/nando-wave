# STOP-R6B Learning Compilers And Feedback

Status: `PASS`

Date: 2026-07-22

Code HEAD: `e5a15a4ac45113ce128d285a5cf5be585e61a8df`

Authority: `false`

## Result

```text
trusted causal rows
-> causal adjudication
-> bounded ProtocolMode search
-> executable protocol artifact compiler
-> immutable candidate artifact or ABSTAIN

independently verified delta
-> BackwardWave
-> CandidateCubeField generation g+1
-> frozen competing circuits
-> admission-ready shadow candidate without authority
```

`nando-operator-learning` now owns B1B causal adjudication, the canonical F4
protocol compiler, F5-A executable artifact compilation, BackwardWave, and the
operator generation firewall. The response crate preserves the old module
paths as compatibility facades.

The shared EffectLaw and protocol-facet constants live in
`nando-operator-kernel`. The learning owner imports neither hot runtime nor
admission. Generic phase/coherence algorithms remain in `nando-core`; they
were not duplicated.

`effect_law_v3` and `crystallized_operator.rs` remain response-facade
orchestration because each currently joins cold induction with a real actor or
independent verifier call. Moving either file wholesale would create a second
runtime/proof owner. R8 must split those integration monoliths by symbol after
R6-C establishes the cold learner boundary.

## Code Commit

```text
e5a15a4  refactor: extract operator learning compilers
```

## Exact Gates

Both remote gates ran at exact HEAD with an empty overlay.

```text
nando-operator-learning tests                       PASS
nando-operator-learning Clippy -D warnings         PASS
nando-response-actor historical fingerprint        PASS
response historical failures                         26 known
response effective Clippy diagnostics                 14 known
new response test failures                              0
new response Clippy diagnostics                         0
new background build processes                          0
learning imports runtime/admission                       0
duplicate Wave/coherence implementation                  0
authority                                            false
```

## Structural And Live Gates

```text
learning isolation route                  PASS / authority_ready=false
compiler ownership route                  PASS / authority_ready=false
feedback generation route                 PASS / authority_ready=false
live composite gate                       PASS
eligible_for_local_accept                 false
response M3                               WATCH
response ACTIVE packages                  0
response false accepts                    0
response runtime parity failures          0
```

The live gate is read-only. It still blocks response authority on no ACTIVE
response package and insufficient M3 windows/coverage. No threshold, registry,
service, deployment, or authority state changed during R6-B.

## Size Result

```text
nando-response-actor/src before R0       103,389 tracked Rust lines
nando-response-actor/src after R6-A       73,815 tracked Rust lines
nando-response-actor/src after R6-B       68,294 tracked Rust lines
nando-operator-learning/src               19,037 tracked Rust lines
```

R6-B removed another 5,521 lines from the facade boundary and 35,095 lines
since R0.

## Receipts

- `R6B_LEARNING_EXACT.json`
- `R6B_FACADE_EXACT.json`
- `STOP_R6B_LIVE_GATE.json`
- `stop-r6b-learning-isolation.trace.json`
- `stop-r6b-compiler-ownership.trace.json`
- `stop-r6b-feedback-generation.trace.json`

STOP-R6B unlocks only R6-C. F5-B remains frozen until STOP-R9.
